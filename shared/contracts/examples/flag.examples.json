[ { "key": "capture.camera", "default": false, "scope": "engine", "description": "camera source: presence/away first", "minTier": "T0" },
  { "key": "surface.block.pinned-doc", "default": false, "scope": "surface", "description": "always-visible pinned doc card with copy bar" },
  { "key": "voice.drift", "default": false, "scope": "mode", "description": "register comparator + escalation chains", "minTier": "T2" },
  { "key": "distill.enabled", "default": false, "scope": "engine", "description": "rolling-merge distiller: window capture, interpolate voice, call llm slot, persist distillate", "minTier": "T1" },
  { "key": "distill.moments", "default": false, "scope": "engine", "description": "typed-moment extraction riding the distill pass (commitment/question/decision/artifact); requires distill.enabled", "minTier": "T1" },
  { "key": "distill.index", "default": false, "scope": "engine", "description": "entity extraction + recency×frequency index riding the distill pass; requires distill.enabled (Moment.refs linking also needs distill.moments)", "minTier": "T1" },
  { "key": "distill.transcribe", "default": false, "scope": "engine", "description": "speech-to-text pre-distill stage: base64 audio/* chunks are transcribed via the stt slot into utf8 text before the distill pass; requires distill.enabled (no persistence path for transcribed-but-undistilled text)", "minTier": "T1" },
  { "key": "act.enabled", "default": false, "scope": "engine", "description": "Act pass on session end: compose a follow-up draft from the session's distillates + moments (prepared, never sent); needs distillates to draft anything, so distill.enabled must have run", "minTier": "T1" },
  { "key": "route.detect", "default": false, "scope": "engine", "description": "context-switch detection: focus signals (window title + repo path) and calendar signals (current/imminent Calendar.app event title + attendees) feed the router, which auto-starts/switches sessions into the matched workspace when a candidate sustains dominance; attribution evidence stamped on every auto-started session", "minTier": "T1" },
  { "key": "screen.ocr", "default": false, "scope": "engine", "description": "screen understanding: source:'screen' image frames are recognized via the ocr slot (paddle-serving, or an openai-compat VLM fallback) into an OcrResult + a distillate the standard surfaces read; runs on capture ingest, independent of distill.enabled", "minTier": "T1" },
  { "key": "workflow.enabled", "default": false, "scope": "engine", "description": "run the drain + session-end pipeline through the workflow executor against the seeded workflow-default document instead of the hardcoded direct-wiring; behavior-identical when ON (same distill/act flags honored), the legacy path when OFF (P4A executor v0)", "minTier": "T1" },
  { "key": "act.tasks", "default": false, "scope": "engine", "description": "task-extract act: on each drain (riding the distill pass), extract follow-up items from the session's accumulated distillates+moments into an editable to-do document; a follow-up draft un-constrains them via {{todo}}. Requires workflow.enabled + distill.enabled (P4A dynamic-to-do seam)", "minTier": "T1" },
  { "key": "distill.fields", "default": false, "scope": "engine", "description": "fast fields (#61): on newly transcribed material (the distill accumulation seam), fan out every triggered fast-tier prompt document CONCURRENTLY against the llm slot; each result publishes field.updated and persists the field's latest value (provisional). Rides the transcribed batch, so it needs distill.enabled + distill.transcribe to have text", "minTier": "T1" },
  { "key": "distill.judge", "default": false, "scope": "engine", "description": "judge stage (#62): at a LOWER cadence than the fast fan-out, review every judge-tier prompt document against the SAME source window PLUS the fast result set; confirm/correct/flag each reviewed field in place and stamp the overrule provenance (which judge, which template, what changed). Tier-gated on fabric contents — with no judge-capable llm endpoint (named llm.judge) it never schedules and fields stay provisional. Requires distill.fields to have produced values to judge", "minTier": "T2" },
  { "key": "ingest.gdoc", "default": false, "scope": "engine", "description": "Google Docs pin ingestion (read-only auth)" },
  { "key": "summaries.enabled", "default": false, "scope": "engine", "description": "hierarchical summaries (#177): at the drain/distill cadence produce rolling + episode + five-minute summaries over the existing distillates (bounded lower-level inputs + selective evidence, refs only), flush a durable session summary at session end, and fold each session into a superseding cross-session project summary (prior revisions retained). Each level is a summary prompt DOCUMENT (cadence/bound/prompt as config, resolvable per workflow/app scope). Model prose is a proposal; when the summarizer is unavailable the summary is persisted degraded (explicit reason, no fabricated prose). Requires distill.enabled to have produced distillates", "minTier": "T1" },
  { "key": "guard.egress", "default": false, "scope": "engine", "description": "egress guard (#63): on every hop MARKED egress (content leaving the machine), run the fabric guard slot as a content/PII filter BEFORE any bytes leave — redact-and-continue masks flagged spans (default), hold-and-surface suspends the hop for release/deny (strict). An empty guard slot fails closed per the GuardPolicy (strict holds; default holds unless unguarded egress is acknowledged). Local-only hops never invoke it. Verdicts (span descriptors, never the raw value) land in the audit ledger", "minTier": "T1" } ]
