{
  "$id": "QueryResult",
  "additionalProperties": false,
  "type": "object",
  "required": [
    "source",
    "items",
    "truncated"
  ],
  "properties": {
    "source": {
      "anyOf": [
        {
          "const": "relevant-now",
          "type": "string"
        },
        {
          "const": "moments",
          "type": "string"
        },
        {
          "const": "ledger",
          "type": "string"
        },
        {
          "const": "sessions",
          "type": "string"
        },
        {
          "const": "pins",
          "type": "string"
        },
        {
          "const": "entities",
          "type": "string"
        },
        {
          "const": "todos",
          "type": "string"
        },
        {
          "const": "drafts",
          "type": "string"
        },
        {
          "const": "teach",
          "type": "string"
        },
        {
          "const": "distillates",
          "type": "string"
        },
        {
          "const": "fields",
          "type": "string"
        },
        {
          "const": "queue",
          "type": "string"
        },
        {
          "const": "transcript",
          "type": "string"
        },
        {
          "const": "senses",
          "type": "string"
        },
        {
          "const": "live-senses",
          "type": "string"
        }
      ]
    },
    "items": {
      "description": "hydrated rows; element shape is keyed by `source` (fields → FieldValue, #61; transcript → TranscriptInspector, #101; senses → SenseGateChain, #7/#101; live-senses → existing SenseLaneSnapshot rows, #174)",
      "type": "array",
      "items": {}
    },
    "top": {
      "minimum": 1,
      "maximum": 50,
      "type": "integer"
    },
    "truncated": {
      "description": "true when more rows existed than were returned under `top`",
      "type": "boolean"
    },
    "suppressed": {
      "minimum": 1,
      "description": "how many rows a user DISMISSED were excluded from these results (#66). Present only when > 0, so a block that emptied purely via suppression can disclose it in its empty-state — explainable, not mysterious.",
      "type": "integer"
    },
    "noCurrentSession": {
      "description": "true ⇒ this query asked for `session: \"current\"` but NO session is live this process (#210/#215), so a session-scoped source read honest-empty rather than the previous session's rows. Present only when true. Lets a block distinguish its \"no session running\" empty state (start a session) from a \"session live but nothing captured yet\" empty — the two truths hud-voice requires be told apart. Absent ⇒ not that state (a live session, or a workspace-level source that carries no session dimension).",
      "type": "boolean"
    }
  }
}
