{
  "$id": "QueryResult",
  "additionalProperties": false,
  "type": "object",
  "required": [
    "source",
    "items",
    "truncated"
  ],
  "properties": {
    "source": {
      "anyOf": [
        {
          "const": "relevant-now",
          "type": "string"
        },
        {
          "const": "moments",
          "type": "string"
        },
        {
          "const": "ledger",
          "type": "string"
        },
        {
          "const": "sessions",
          "type": "string"
        },
        {
          "const": "pins",
          "type": "string"
        },
        {
          "const": "entities",
          "type": "string"
        },
        {
          "const": "todos",
          "type": "string"
        },
        {
          "const": "drafts",
          "type": "string"
        },
        {
          "const": "teach",
          "type": "string"
        },
        {
          "const": "distillates",
          "type": "string"
        },
        {
          "const": "queue",
          "type": "string"
        }
      ]
    },
    "items": {
      "description": "hydrated rows; element shape is keyed by `source`",
      "type": "array",
      "items": {}
    },
    "top": {
      "minimum": 1,
      "maximum": 50,
      "type": "integer"
    },
    "truncated": {
      "description": "true when more rows existed than were returned under `top`",
      "type": "boolean"
    },
    "suppressed": {
      "minimum": 1,
      "description": "how many rows a user DISMISSED were excluded from these results (#66). Present only when > 0, so a block that emptied purely via suppression can disclose it in its empty-state — explainable, not mysterious.",
      "type": "integer"
    }
  }
}
