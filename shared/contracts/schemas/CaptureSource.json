{
  "$id": "CaptureSource",
  "description": "mic = the user; system-audio = the far side of a call (loopback) — the free me/them split",
  "anyOf": [
    {
      "const": "mic",
      "type": "string"
    },
    {
      "const": "screen",
      "type": "string"
    },
    {
      "const": "calendar",
      "type": "string"
    },
    {
      "const": "repo",
      "type": "string"
    },
    {
      "const": "camera",
      "type": "string"
    },
    {
      "const": "system-audio",
      "type": "string"
    }
  ]
}
