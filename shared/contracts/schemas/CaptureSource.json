{
  "$id": "CaptureSource",
  "description": "mic = the user; system-audio = the far side of a call (loopback) — the free me/them split; focus = foreground-window context (P3 context-switch detection), utf8 JSON FocusSignal, never a transcript",
  "anyOf": [
    {
      "const": "mic",
      "type": "string"
    },
    {
      "const": "screen",
      "type": "string"
    },
    {
      "const": "calendar",
      "type": "string"
    },
    {
      "const": "repo",
      "type": "string"
    },
    {
      "const": "camera",
      "type": "string"
    },
    {
      "const": "system-audio",
      "type": "string"
    },
    {
      "const": "focus",
      "type": "string"
    }
  ]
}
