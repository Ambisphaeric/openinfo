# client/main — Phase 1 (glass transplant)
Electron main process: `windows.ts` (content-protection, hide-from-screenshare, ⌘\),
`tray.ts`, `shortcuts.ts`. The menu bar app maps 1:1 to settings documents — no crossed wires.
