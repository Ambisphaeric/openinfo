# client/capture — Phase 1 core (glass transplant), Phase 7 growth
- `mic.ts` (P1) · `screen.ts` (Δ-diff gate) (P1) · `audio-system.ts` (SystemAudioDump) + `aec/` (P1–2)
- `calendar.ts` (P2, read-only) · `focus.ts` (P3, window/repo focus polling)
- `camera.ts` (P7, flagged) — presence/away detection first, document capture second
Inputs are user-configurable sources: each exposes on/off + cadence to the palette (P6).
