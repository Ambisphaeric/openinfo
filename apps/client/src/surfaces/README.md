# client/surfaces — Phase 2 (renderer + HUD), Phase 6 (editors)
- `block-renderer/` (P2) — renders a surface DOCUMENT into a panel. No hardcoded layouts, ever.
- `blocks/` (P2+) — built-ins: now, moments, relevant-now, ledger, pinned-doc, hint…
- `hud/` (P2) — the live-join panel (renderings: states A/B/C)
- `palette/` (P6) — user-designed visible buttons + input toggles (mic/screen/camera)
- `settings/` (P2 minimal) — engine address, fabric, flags
- `editor/` (P6) — surface WYSIWYG + mode form/canvas + register/dial editor
