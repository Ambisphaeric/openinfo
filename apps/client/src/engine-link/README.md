# client/engine-link — Phase 1
Typed HTTP/WS client generated from contracts. `spool.ts`: offline buffer when the engine is
unreachable (the seam demo: unplug, spool, replug, catch up). Engine address is user config.
