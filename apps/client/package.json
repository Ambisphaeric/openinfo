{
  "name": "@openinfo/client",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/main/shell.js",
  "scripts": {
    "build": "tsc -p .",
    "test": "node --test 'dist/**/*.test.js'",
    "start": "pnpm build && electron ."
  },
  "dependencies": {
    "@openinfo/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "electron": "^38.0.0",
    "typescript": "^6.0.3"
  }
}
