# engine/flags — Phase 0
Flag registry. A flag is a document { key, default, scope: engine|surface|mode, description },
settable per user and per context. Every feature ships OFF until its phase exits.
