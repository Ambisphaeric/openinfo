# engine/workflow — Phase 2 (loom transplant)
loom packages/recipe (DAG executor) + `compile.ts`: mode document → executable DAG.
The five processing primitives (source/distill/route/overlay/act) are the only node types.
