# engine/ledger — Phase 4
- `commitments.ts` — lifecycle: open → prepared → done; born-with-context records
- `watchers/repo.ts` (first) · `watchers/doc.ts` · `watchers/mail.ts` (heuristic before auth)
- `prepare.ts` — action cards: draft the artifact, attach it, expose the copy verb
"Did I?" is asked only when every watcher comes back empty.
