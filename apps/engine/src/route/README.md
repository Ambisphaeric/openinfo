# engine/route — Phase 3
- `detector.ts` — context-switch detection (window title + repo path + calendar + voices)
- `attribute.ts` — session → workspace, with evidence recorded on the session
- `identity.ts` — user's name/aliases; person resolution (diarization behind a flag, P7)
- `reroute.ts` — one-click retroactive move of a session between workspace DBs
