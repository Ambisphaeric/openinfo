# design/renderings — versioned HTML mockups

Self-contained HTML (no external deps) — open in a browser or iterate directly; these are the
design source of truth for surfaces until real ones ship.

| file | contents |
|---|---|
| surfaces-v0.2.html | 7 surfaces + code map: HUD w/ rewind, mode config (hardware envelope, remote engine), pipeline canvas, backlog analytics, timeline, explore canvas, inbox |
| hud-v2.html | The HUD as a product: live-join states A (security review) / B (deep work) / C (horizon ledger), context index, block composition |

Convention: never overwrite a version — copy to `-vN+1` and iterate there.
