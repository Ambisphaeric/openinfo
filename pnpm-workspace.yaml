packages:
  - shared/*
  - apps/*
  - tools/fixtures
  # spikes/ and templates/ are deliberately NOT workspace packages:
  # spikes are throwaway and must not be importable; templates are documents, not code.
