# openinfo — code map

**Status:** Phases 0–2 built + P3 in progress (contracts · seam · distill/moments/index · sessions · HUD · act · fabric profiles/secrets · the Settings sidebar at GET /settings (Status·Get-started·Endpoints·Profiles·Keys·Local-runtimes·Features·HUD-layout·Benchmarks·Try-it·Privacy; /setup 301s here) · onboarding discovery + Get-Started lens · the "watch it become a moment" Try-it loop · engine-managed local runtimes / tier zero · client system-audio capture (BlackHole detect-and-guide) · rig-truth fabric defaults + managed omlx/mlx (P4-T8) · **0.0.1: an installable arm64 DMG that isn't a dead shell — the shell adopts a running engine or spawns the bundled one on first launch**) · 2026-07-09
**Reads with:** [ARCHITECTURE.md](./ARCHITECTURE.md) (the what) · [IMPLEMENTATION.md](./IMPLEMENTATION.md) (the when)
This file is the **where** — including where features that don't exist yet will land, so no later phase ever
has to invent a home (the historical failure mode).

---

## 1. The tree

```
openinfo/
├─ shared/contracts/            P0   the only shared dependency; typed seam
│  ├─ src/records/              P0   commitment, moment, entity (**#73** contract v2: append-only `confidence`/`state`/`ambiguity`/`heardAs[]`/`sightings[]`/`overrides[]`/`external` — evidence trail + sovereign user overrides for the resolver #72; all optional ⇒ v1 rows still validate; sub-schemas Sighting/HeardAs/EntityOverride/EntityAmbiguity/EntityExternal registered in AllSchemas; **#72** adds `EntityResolution` sub-schema + optional `resolutions[]` — the scored resolver's append-only per-mention trail; **#116** additive `spanId` correlation ids on Distillate/Moment/EntityProvenance/OcrResult/FieldValue/JudgeReview/GuardHold + parent links `FieldValueProvenance.sourceChunks`/`GuardHold.sourceChunks` + optional `usage` on FieldValueProvenance/JudgeReview — every record of one pipeline pass shares a spanId and every derived record names its material; all optional ⇒ prior rows still validate), **sttSegment (#116: per-transcribed-segment STT provenance — chunkId/spanId/source/capturedAt→processedAt/textChars + endpoint/model/durationMs/egress, deliberately NO transcript text; the trace root that closes the disclosed #65 gap)**, **contextPacket (#176 slice 1: the converged ContextPacket — a REFS-ONLY derived unit over immutable observations: separate mic/systemAudio/screen ref arrays [source identity never merges], optional focus/app evidence from the Session record, entity candidates with #74 screen corroboration, machine-readable `gaps` for missing senses, deterministic confidence, append-only `revision`/`supersedes` chain; sub-schemas ContextPacketRef/Gap/Candidate/Provenance in AllSchemas; deliberately NO prose field — prose stays a read-time view over the refs; + `BuildContextPacketsRequest` in api/payloads)**, session, workspace, pin
│  ├─ src/config/               P0   surface(block/action; **#99** optional append-only `Surface.workspaceId` — an app INSTANCE = a surface bound to a workspace silo, the DEFAULT workspace for its block queries), mode, voice, fabric, flag · workflow (P4A: WorkflowSpec — ordered flag-gated steps document; the pipeline as a cloneable doc, editable over GET/PUT /workflows since P4-T1) · todo (P4A-s4: TodoList/TodoItem — the editable session to-do document the constrain/unconstrain loop accumulates into) · **#134** two new surface PRIMITIVES: the `input` BlockTypeName + `Block.input` {target, submit, mode text/file/both, placeholder/accept/submitLabel} (text-entry/file-drop as a document field), and `Surface.panel` (AttachedPanel {edge below/right, collapsed/expanded px, reveal user/event, openOn, startExpanded} — the attached-expansion-panel geometry, both append-only/optional) · **raw-frame trusted hosts:** the http `Endpoint` variant (fabric) gains optional `trustRawFrames` — the user's EXPLICIT declaration that this host may receive raw screen frames; absent ⇒ raw frames stay loopback-only (default unchanged), honored only for LAN-local hosts (a public host is denied even when set — the cap is absolute); append-only/optional, per-endpoint user config, never auto-set
│  ├─ src/api/                  P0   HTTP routes + WS events (names, payloads) · **#134** POST /chat (ChatRequest {message, workspace?, pinId?, history?} → ChatReply {answer, citations[], budget: honest turn/context budget, egress}) · **Ask face:** ChatRequest gains optional `screenshot` (ChatScreenshot {contentType,data} — one base64 frame per explicit send) + `turnId` (client-minted stream key); `ChatDelta` {turnId,seq,text,done} behind the ephemeral `chat.delta` WS event (the #58 idiom — never persisted, the ChatReply is authoritative); `ChatHistory` {turns,total,truncated} behind GET /chat/history (registered)
│  │  └─ senses.ts             P4   **#174 live physical-lane contract:** closed mic/system-audio/screen source union; metadata-only capture + processing evidence; atomic `SenseLaneSnapshot` rows and canonical three-row `SenseLaneSnapshotSet`. Slice C adds the closed client `ScreenCaptureObservation` union (`queued`/`delta-skipped`/`grab-failed`), screen-only `ScreenLaneObservation` derivation evidence (`id`/`occurredAt`/skip-or-grab outcome), and internal `ScreenProcessingOutcome` (`processed`/`blank`/`failed`), with capture/time correlation but no pixels, derived text, endpoint/model detail, or arbitrary errors. A screen row's optional `latestObservation` explains a visible delta skip or capture failure; queued truth remains explained by `latestCapture`. **#192** the client observation union's failed-attempt variant widens to `grab-failed`/`permission-denied` (the OS-refused-run report, still attempt-correlation only) and `ScreenLaneObservation.outcome` mirrors it — closed literals, no new field: lane rows stay metadata-only with the reserved `blocked` health + `disabled`/`permission-denied`/`configuration-blocked` reasons now emitted by real producers. `routes.ts` registers authenticated `GET /senses/live` and `POST /screen/observations`; `events.ts` registers public `sense.lane.updated`; every schema is generated under `schemas/`
│  ├─ src/query/                P0   block query DSL (grammar, types)
│  └─ schemas/                  P0   generated JSON Schema — language-neutral (Rust-portable) · **#87** CI schema-drift guard: `.github/workflows/ci.yml` regens (`pnpm --filter @openinfo/contracts gen`) after Build, before Test, and fails on `git diff --exit-code shared/contracts/schemas/` — a contract-source change that skips regen fails loud, naming the stale files. Now ALSO a local test (`src/schema-drift.test.ts`): regenerates into a temp dir (`SCHEMA_OUT_DIR` seam) and diffs against `schemas/`, so `pnpm -r test` catches drift too, with a "run `pnpm gen`" message — #87 CLOSED
│
├─ apps/engine/src/             the daemon (authenticated loopback listener; cross-machine only through a trusted HTTPS tunnel)
│  ├─ api/                      P1   **#123 control plane:** control-plane.ts (secure-by-construction local/tunnel modes; loopback-only bind; per-launch local Bearer; owner-only atomic `~/.openinfo/run/engine-<port>.json`; exact Host/Origin/CORS), browser-auth.ts (one-use ~30s Settings ticket → in-memory HttpOnly SameSite=Strict cookie, Secure through tunnel), http.ts (only GET /health public; every other read authenticated; every POST/PUT/DELETE JSON; guard order Host→target→Origin→preflight→auth→media type→parse), ws.ts (Bearer or `openinfo.auth.<token>`, no URL credential; public capture event is metadata-only CaptureReceipt while the internal bus retains CaptureChunk) · http.ts (all route handlers, one dispatcher; **pill P2:** GET/PUT `/active-preset`[`?workspace`] in the template/preset region — read the selection + the five choices, set with honest validation [nonexistent ⇒ 400, null clears]; handled-but-unregistered in the /routes manifest, consistent with the guard control-plane pair; **#176:** GET `/context/packets`[`?workspace&session&from&to&entity&superseded`] — the four packet query axes, live chain heads by default — + POST `/context/packets/build` [BuildContextPacketsRequest → runs the deterministic builder over the session's stored records, persists + returns ONLY the appended packets; [] = the honest idempotent no-op; unknown workspace/session ⇒ 404], both registered in the /routes manifest) · json/validation/defaults · **#134** chat.ts (POST /chat: the below-HUD chat shell's turn — `runChat` gathers each declared source's data over a narrow `ChatDeps` and invokes the llm slot egress-gated [resolveEgress content-class `typed` + the #63 guard], `computeBudget` returns the HONEST visible turn/context budget whose `note` now prepends the per-source assembly disclosure; a failure THROWS → the route returns a 502 whose `error` the input block paints) · **pill P1** context-assembly.ts (the chat route READS the governing bundle's `chat.contextAssembly` DECLARATION — `assembleChatContext` iterates the ordered sources honoring each `limit`/`windowChars`/`tokenBudget`, composes the system block + `historyTurns` + citations, and returns one honest `SourceReport` per source [included/capped/empty/unavailable] — assembly is DATA, not code; `describeAssembly` renders the visible note; `DEFAULT_CONTEXT_SOURCES` is the no-plan fallback; the P2-owned **active-preset read-seam** `ActivePresetRef`/`resolveActivePreset?` is WIRED at merge to `ctx.presets.resolveActive` (pill P1×P2 integration — preset doc→{label,text}), so the source reports `empty` when unset and `included` once a workspace selects a preset. POST /chat resolves the seeded Standard App bundle → a bundle-doc edit [PUT /bundles] changes assembly with NO code change; its `transcript-window` gather filters the process-global diagnostic ring through the requested workspace's currently-owned session ids, so reroutes follow current ownership and cross-workspace transcript text cannot enter the prompt) · **Ask face:** chat.ts grows three OPTIONAL ChatDeps seams — `screenText` (frame→text; a read failure NEVER fails the send), `recentTurns` (the persisted thread beats request.history; request is the fallback against an empty store), `onDelta` (streaming) — and context-assembly gains the eighth `screen` source (no frame ⇒ empty · unreadable ⇒ unavailable · text ⇒ included/capped under the declared budget, DEFAULT_SCREEN_CHARS 4000). postChat reads the frame via invokeOcr under content-class `screen` consent (raw frame bytes are LOOPBACK-ONLY by default — managed local runtime or localhost/127/::1; a user may explicitly trust a private-LAN HTTP OCR/VLM endpoint with `trustRawFrames:true`, but public destinations remain denied; only derived TEXT rides the typed-gated #63-guarded chat hop), publishes ephemeral `chat.delta` frames per model chunk (terminal done:true in a finally; ws-rebroadcast beside transcript.updated), persists the exchange to the workspace thread AFTER success, and GET /chat/history serves the oldest-first tail (CHAT_HISTORY_LIMIT 50, total+truncated disclosed)
│  ├─ senses/                   P4   **#174 runtime truth projection (BUILT through slice C):** `live.ts` is the pure, process-local `SenseLaneTracker` for mic/system-audio/screen. It starts stopped even when an unended session persists on disk; only lifecycle events observed this launch open lanes. Physical media receipt moves the source-local latest capture to queued; authenticated screen observations truthfully project `queued`, `delta-skipped`, or `grab-failed` (the last as failed/capture-failed); processor evidence projects `blank` or failed/processing-failed; and exactly correlated transcript/OCR completion moves a lane to processed with fixed-basis lag. A visible delta skip/capture failure retains its exact attempt derivation as screen-only `latestObservation`; a visibly newer physical capture clears it, while older late evidence cannot erase it. `ocr.completed` is the screen lane's sole processed-success signal. Exact capture/time correlation, terminal ordering, and idempotency prevent retries, regrouping, stale observations, or a later failure from regressing success. Raw bytes, derived text, endpoint provenance, and arbitrary errors never enter the tracker. **#210** `currentSessionId(workspaceId)` exposes the tracker's runtime-current session id (its `currentByWorkspace`, `!ended`-guarded) as the shared authority for honest display scope — the record-query resolver (`surfaces/query.ts` `resolveQueryScope`) and the HUD's live-session listing (`api/http.ts` `readSessions`) both bind `session:'current'` to it, so a stale persisted session never renders as current. `api/http.ts` wires lifecycle/capture/completion events, authenticated `GET /senses/live`, authenticated JSON `POST /screen/observations`, and metadata-only `sense.lane.updated`; WS egress revalidates the closed schema and drops invalid/extra-field payloads; **#174 slice E** `live-replay.test.ts` is the deterministic tri-lane proof — the committed `tools/fixtures` synthetic-converged fixture replayed through the REAL `transcribeChunks` + `ScreenOcrProcessor` stages into this tracker, asserting three distinguishable lanes each correlated to ITS OWN replayed capture id (mic/system-audio/screen, exact equality, no merge/swap), byte-stable across two in-process runs, a withheld lane staying honestly idle (never relabeled) + honest session-end, and that `snapshotSet` — the exact projection `GET /senses/live` and the `live-senses` `/query` source both return — matches the shared fixture-derived rows; **#192 real gate state → lane health:** `gates.ts` (`senseLaneGateState`) is the closed bridge from the REAL per-sense gate chain (`surfaces/settings/sense-gates.ts` `evaluateSenseGates`) to the tracker — a blocking flag gate reads `disabled`, a missing slot/workflow screen step reads `configuration-blocked`, `*-health` gates never overlay (deterministic configuration only, no probe); `live.ts` `applyGates` holds the engine-global overlay APPLIED AT PROJECTION over the untouched underlying reducer (only health/reason mask; disposition + capture/processing evidence stay truth), so reopening a gate restores the exact prior state without restart, cold/ended lanes are never gate-relabeled, junk reasons fail closed, and the verdict is idempotent (only changed lanes emit); `api/http.ts` re-evaluates on `flag.changed`/`fabric.changed`/`workflow.updated` + seeds at boot; a `permission-denied` screen observation projects `failed`/`blocked`/`permission-denied` with exact attempt provenance and the next accepted capture restores queued truth — proven at unit (`live.test.ts`, `gates.test.ts`), replay (`live-replay.test.ts` no-relabel + restore extensions), and route level (`api/sense-lanes.test.ts`: fresh-default disabled truth, configuration-blocked → restored without restart, denial not idle, one-lane disable relabels nothing, closed public rows)
│  ├─ bus/                      P1   ← loom packages/bus
│  ├─ store/                    P1   ← loom packages/store + sqlite-vec
│  │                                 workspace-registry (DB-FILE PER WORKSPACE) · sessions (P2: manual start/stop lifecycle) · distillates/moments/entities/drafts (P2) · **stt_segments (#116: saveSttSegment/listSttSegments mirror saveDistillate — per-workspace table, additive DDL, carried by moveSession)** · **context_packets (#176: saveContextPacket [contract-validated, append-only chain rows, insert-or-replace on the content-derived id] / listContextPackets [the four query axes: session · time-window intersection · related entity · supersession resolved over the whole scope BEFORE filters so a filtered read never presents a superseded packet as live; `includeSuperseded` keeps history reachable]; additive DDL; moveSession carries the WHOLE chain, superseded revisions included)** · graph (P3) · layouts (P2) · **pill P2 active-preset selection:** `active_preset` column on the workspaces meta table (additive migration in place, the egress idiom) + `getActivePreset`/`setActivePreset` — the narrow per-workspace read the distiller + chat-assembly (P1) land on; kept OFF the Workspace object/contract (its own column, not `fromRow`); unset ⇒ undefined ⇒ no injection · **Ask face ask-history:** `chat_turns` table per workspace DB (create-if-absent in the open path — existing DBs gain it on next open; autoincrement `seq` keeps same-millisecond exchange order) + `appendChatTurn` (Tier-A ChatTurn-validated, ensure-workspace) / `listChatTurns` (oldest-first, limit keeps the honest tail) / `countChatTurns` — the persistent per-workspace chat thread (workspace-level like pins, no part of a session move)
│  ├─ fabric/                   P1   slots stt/tts/llm/vlm/ocr/embed · endpoints/local.ts (P2 tier zero: LocalRuntimeManager) · http/cloud inline in invoke/health · stt-adapters.ts (P4-T9: canonical TranscriptResult + per-flavor response adapters — whisper-server/openai/omlx — chosen by api/runtime) · **#65 token accounting:** invoke.ts `buildUsage`/`estimateTokens` capture `InvokeUsage {promptTokens?,completionTokens?,totalTokens?,estimated,durationMs?}` per invoke — MEASURED from the openai-compat `usage` block, else chars/4 ESTIMATE + marked; carried on `LlmResult`/`ScreenTextResult` (generic, so the #62 judge through invokeLlm gets it free), threaded into distillate/ocr provenance. STT not wired (no provenance-record home) · **#64 layered egress consent:** egress.ts — `classifyEndpoint`/`classifyHost`/`classifyDestination` (layer 1: loopback/LAN/spawned = local, hosted/cloud = egress, fail-closed on an unparseable url) + strict `isLoopbackEndpoint` (managed local runtime or localhost/*.localhost/127/::1 only; wildcard/LAN/mDNS/public denied) + `mayReceiveRawFrames` (the raw-frame predicate: loopback OR an http endpoint the user explicitly flagged `trustRawFrames` on a LAN-local host — a flagged public host or wildcard bind stays denied; the LAN cap is absolute) + `resolveEgress(ctx)→{allowed,decidedBy,reason}` (layers 2-4: prompt.neverEgress · mode/workspace.egress.deny · content-class screen — MOST-SPECIFIC denial wins: content-class > prompt > mode > workspace, else `default`) + `egressDecision(reach,consent,destination-detail)` for provenance (device-local vs LAN-local vs hosted-public; trusted raw-frame LAN also stamps `rawFrameTrust:"explicit"`, never URL/secret); invoke.ts `egressGate` runs per candidate endpoint BEFORE any bytes leave — ordinary LLM/STT content preserves the broader local/LAN policy, while OCR/VLM raw frames are loopback-only BY DEFAULT — reaching a LAN host requires that endpoint's explicit `trustRawFrames` opt-in, and a flagged public host is still denied (the skip line names the real reason each way); disallowed endpoints are SKIPPED with an `egress-denied` classified failure before runtime-resolution/auth/fetch; the `{allow:true,reach:'egress'}` return is the **guard #63 hook (BUILT):** `invokeLlm` runs `runEgressGuard` (`fabric/guard.ts`) there — redact-and-continue masks flagged spans on the outbound copy + proceeds, hold-and-surface/fail-closed throws `GuardHeldError` (denial still short-circuits before the guard); decision stamped on `LlmResult`/`SttResult`/`ScreenTextResult` when consent supplied, threaded into distillate/ocr provenance. **#175 raw-frame failure containment:** OCR/VLM non-OK responses are body-cancelled UNREAD before status-only classification, so a server echo of the submitted base64 cannot propagate through `serverMessage`, aggregate errors, queue/status state, or logs. Factory posture: no egress endpoint ⇒ nothing leaves · **Ask face streaming:** `InvokeOptions.onDelta` — the completions request asks for SSE (`stream:true`) and each content delta forwards as it arrives (readSseCompletion; reasoning-exhausted classified like the buffered path, SSE usage measured else estimated+marked); the RESPONSE content-type decides, so a server that ignores the flag degrades honestly to one chunk (no fake typewriter); without onDelta the body is byte-for-byte the legacy stream:false shape; once any delta was emitted a mid-stream failure SURFACES (no silent fall-through to the next endpoint)
│  │                                 bench (measured tok/s; local stays stubbed — real numbers need hardware) · health (first-healthy-wins; reports local spawn state; openai-compat root-404 falls back to GET /v1/models — FastAPI-style servers serve no root) · invoke (P2: llm chat-completions · stt /v1/audio/transcriptions multipart — both openai-compat, first-healthy-wins; keyRef→Authorization: Bearer at invoke time; local branch spawns the runtime + speaks its surface — whisper.cpp /inference; a MISSING message.content with reasoning tells classifies reasoning-exhausted, not bad-response — some servers omit the key instead of sending ''; wired-up-arc P1: per-endpoint request extras `chatTemplateKwargs`/`responseFormat` (also on `InvokeOptions` as an override) go into the completions body ONLY when set — `{enable_thinking:false}` stops qwen3.5-9b burning the distill budget; never auto-set)
│  │                                 profiles (named/versioned/cloneable slot-maps; active = live fabric; GET/PUT /fabric = active view) · secrets (SecretStore interface; v0 chmod-600 JSON in secrets/, keychain P7 — write-only API, refs never values)
│  │                                 discover (P2: probe-list + capability-map docs → GET /fabric/discover: parallel probe /v1/models, classify by name, synthesize a config-1 suggestion — smaller-model-first ranking; LAN sweep later)
│  │                                 scan (P3: POST /fabric/scan — user-directed host-scan for the Endpoints editor: exact url, or bare host × the probe-list ports; classified via the invoke taxonomy; value-free re keys; never cached)
│  │                                 local runtimes (P2 tier zero: endpoints/local.ts spawn/health/kill/bounded-restart of llama.cpp/whisper.cpp · local-models.ts download+resume+size-check · local-{documents,defaults}.ts starter catalog · GET /fabric/local/models · POST /fabric/local/download · P4-T8: mlx/omlx spec — adopt-only/multi-model on fixed :8000, keyRef auth on the local kind)
│  ├─ workflow/                 P2   ← loom packages/recipe · compile.ts (mode doc → DAG) — DAG still deferred until multi/chained acts (see workflow/README). P4A slice 1: the WorkflowSpec CONTRACT (shared/contracts config/workflow.ts) + seeded workflow.default.json. P4A slice 2 (BUILT): executor.ts (WorkflowExecutor — runDrain/runSessionEnd over the document, coalesces the distill family into one distillChunks call, propagates throws for retry-at-idle) · documents.ts (WorkflowDocuments store, seeds workflow-default, read fresh per call; P4-T1: additive `list()` + version-stamped, contract-validated `save()` — the Tier-A gate that rejects an unrunnable step kind at write time) · defaults.ts. P4-T1 (BUILT): GET/PUT `/workflows` routes in api/http.ts (list · get 404-unknown · put validated+version-bumped, unknown-id CREATES like PUT /todos) — the pipeline is user-composable over the API, ZERO executor change (the hot read seam needed only the HTTP surface). Gated by workflow.enabled (default OFF): OFF = legacy api/http.ts wiring untouched, ON = behavior-identical. P4A×P4B JOINT SLICE (BUILT): the ocr/vlm drain steps are REAL drain stages via the injected `recognizeScreen` seam (= screen/processor.runOnDrain over invokeOcr/invokeVlm) — run FIRST, before the distill gate, independent of distill.enabled, gated per-step by `when` (screen.ocr), throws PROPAGATE (re-queue). Seeded default now carries a `screen-ocr` step (behavior-identical when workflow.enabled OFF) · #121 (BUILT): ports.ts (STEP_PORTS — total, data-only ports metadata per WorkflowStepKind: inputs/outputs/emits named by contract $id; the editor-arc precursor the WorkflowSpec DAG-deferral note reserves; totality double-enforced — Record type + schema-derived kind list in ports.test.ts; no route/flag/behavior change)
│  ├─ bundles/                  P4   bundle-as-runtime-object (BUILT): documents.ts (`BundleDocuments` store — same `_meta.db` LayoutStore its sibling doc kinds live in; seeds the Standard App only-when-absent, `list`/`get` with a code-default fallback, version-stamped contract-validated `save` — the Tier-A gate that rejects an unrunnable face kind / ungatherable chat source at write time) · defaults.ts (`loadDefaultBundle` from the SAME validated `bundle.standard-app.json` the contract slice seeded — one source of truth, mirrors workflow/defaults). GET/PUT `/bundles`[`/:id`] routes in api/http.ts (list · get 404-unknown · put validated+version-bumped, unknown-id CREATES — mirrors /workflows). `createEngineApp` seeds on boot (ensureDefaults). The Standard App composes ONLY existing organs (faces → surf-openinfo-pill [the hud/glance face — THE PILL, see the-pill slice]/-chat/-fields/-diagnostics, workflowRef workflow-default, templateRefs distill/extract/entities/follow-up, flag overlay, the 7-source chat context-assembly plan) — proven by bundles/seed-integrity.test.ts (every ref resolves to a served organ) · **Ask face:** ensureDefaults grew the #130 seed-or-refresh discipline — an UNEDITED v1 seed (byte-identical to a `PREVIOUS_DEFAULT_BUNDLE_BODIES` entry) refreshes to the new shipped doc (v2, at most once) so an existing install's chat plan gains the eighth `screen` source; any user edit is never clobbered; the shipped plan now declares 8 sources
│  ├─ presets/                  P4   pill P2 — the five editable context presets, ACTUALLY injected (BUILT): defaults.ts (the glass-parity five — School/Meetings/Sales/Recruiting/Customer-Support — as `kind:'preset'` `PromptTemplate` DOCUMENTS, SIMPLE #130 one-sentence neutral bodies, no baked voice vector, no `slot`) · documents.ts (`PresetDocuments` over the SAME `prompt-template` store kind — so presets enumerate over GET /templates and EDIT over PUT /templates/:id with NO new doc kind / editing UI; `ensureDefaults` seed-if-absent, `list`/`get` kind-guarded, `isPreset` PUT-validity gate, `activeId` raw seam, and `resolveActive(workspaceId)` — the ONE "resolve the workspace's active preset if set" read the distiller injection AND the chat context-assembly path (P1) both consume). Selection itself is per-workspace store state (store/workspaces `getActivePreset`/`setActivePreset`); GET/PUT `/active-preset`[`?workspace`] routes in api/http.ts (read {workspaceId,presetId|null,presets[5]} · set with honest validation: nonexistent preset ⇒ 400, non-string non-null ⇒ 400, null clears). INJECTION at distill/distiller (see below). Unset ⇒ byte-identical to today
│  ├─ distill/                  P2   merge · distiller · transcribe (audio→text pre-distill drain stage via stt slot; mic="me"/system-audio="them" speaker split; #58: onTranscribed hook + buildTranscriptUpdates → ephemeral transcript.updated fast-path, NOT persisted; **#116:** onTranscribed also hands `SttInvokeMeta` — endpoint/model + a MEASURED invoke durationMs — and the http.ts wiring persists one SttSegment per transcribed chunk, so the durable trace root rides the same seam) · echo-dedupe (PR #151: pure per-session 30s buffer of system-stream fragments; a mic fragment near-duplicating one within ±2s — Jaccard ≥0.8 or mic⊆system containment; <3 unique-token fragments never dropped — dies at the runTranscribe seam BEFORE text-queue persistence AND the transcript.updated fan-out; `OPENINFO_ECHO_DEDUPE=0` kill-switch; suppressed count log-only until a System-app surface reads it) · cadence (#58 DistillCadence: accumulate transcribed text per session, release the distill LLM pass only at ≥15s capturedAt span OR session-end flush — transcription still runs every drain) · moments (typed extraction) · parse (defensive JSON, shared) · defaults/documents (template+mode docs; **#61** seeds 3 fast-field prompt docs topic/entities-mentioned/work-items + `fieldTemplates()` reader; **#62** seeds the judge prompt doc `tpl-judge-default` + `judgeTemplates()` reader; **#130** the three window templates (`tpl-distill/extract/entities-default`) ship NEUTRAL — no baked voice vector (`{{tone}}…{{voice.rules}}` stripped from the bodies; the machinery stays live for edited templates + the act draft output); a one-time `seedOrRefreshBuiltin` refresh in `ensureDefaults` updates an UNEDITED builtin to the new body (detected conservatively: version still 1 AND body byte-for-byte one of `PREVIOUS_BUILTIN_BODIES`), NEVER clobbering a user edit) · **#61 fast fields (the fan-out substrate):** fields.ts — `FastFieldScheduler.runFields(chunks)` reads every `kind:'field'` prompt doc (a `PromptTemplate` with a `field` binding `{fieldId,tier,trigger:{kind:'transcript',minChars?},scope}`), applies the per-field minChars relevance gate, runs the TRIGGERED fast-tier ones CONCURRENTLY (`Promise.all`) against the llm slot, then per result publishes `field.updated` + persists the latest value; per-field invoke failure is caught (no event, no value, nothing fabricated); rides the SAME cadence-released batch the distiller sees, gated by `distill.fields`; **#116:** each fan-out pass mints ONE spanId shared by its values and stamps `provenance.sourceChunks` (the batch's chunk ids) + `usage`, the distiller mints one spanId per merge window (distillate+moments+entity mentions+a GuardHold share it; a hold also records its window's sourceChunks), and the judge mints one per reviewed batch (spanId+usage on each JudgeReview). field-values.ts — `FieldValueStore` persists the DURABLE half: one `_meta.db` doc per field keyed deterministically by scope (`fv:<ws>:<session|''>:<field>`), so getLatest IS the current value; contract-validated before write · **#62 judge stage (dual-input review):** judge.ts — `JudgeScheduler.runJudge(chunks)` reads every `judge`-tier prompt doc and, per judge, judges the fast-result set it `reviews` (default: all fast fields in scope) AGAINST the SAME source window the fast tier saw — the standardized `{{source}}`/`{{results}}` template contract — then applies each per-field verdict (confirm→`confirmed`, correct→overrule the value in place→`corrected`, flag→`flagged`), persisting a NEW version of the SAME FieldValue stamped with `provenance.judge` (JudgeReview: judge template/endpoint/model, verdict, priorValue on a correct) + republishing `field.updated`; nothing fabricated (a correct with no value / a verdict for an unknown field is skipped-with-log); a judge invoke failure is caught per document. TIER-GATED via `hasJudgeEndpoint()` — the judge routes to the `llm.judge`-designated endpoint (OPENINFO_JUDGE_ENDPOINT override) in a one-endpoint sub-fabric; with none configured the pass is a logged no-op and fields stay provisional. Wired in http.ts on its OWN cadence (`judgeCadence`, default 4× the distill cadence, OPENINFO_JUDGE_CADENCE_MS) decoupled from the fast fan-out, gated by `distill.judge` · **#131 orientation pass:** `runJudge` routes a `produces:'orientation'` judge doc (seeded `tpl-judge-orientation`, neutral #130 body) to `runOrientation` — classifies the session (nature/direction/topics) from the SINGLE `{{source}}` window into an ENGINE-STAMPED `SessionAnnotation` (additive to Session — a separate record keyed `oa:<ws>:<session>`, so a later pass replaces in place = ANNOTATE-AND-CORRECT; model controls only the classification text, blanks default to `unclear`, topics engine-capped so counts are never trusted), persisted via LayoutStore (`session-annotation`) + emitted as `orientation.updated` (bus→WS, the #134 sidebar trigger); tier-gated exactly like #62. **GATE-READY SEAM:** application is funneled through the single `applyAnnotation`, switched on `orientationDisposition` — `annotate` (shipped) vs `gate` (designed-not-built: hold-until-classified slots into this one method + the drain, no re-architecture; today it annotates + logs) · **pill P2 preset injection (BUILT):** at the one interpolation site where the distill SUMMARY prompt is composed, `distiller` resolves the window's workspace active preset (`PresetDocuments.resolveActive`, optional dep) and PREPENDS its body as leading context — so the selection applies to WHATEVER distill template is in force (not only one carrying a `{{preset}}` placeholder) and unset is EXACTLY byte-identical to today; only the summary is steered (the strict-JSON moments/entities passes stay bare); `Distillate.provenance.presetId` names the preset that shaped a summary (honest why-record, System/Ledger register only) · **Ask face:** defaults.ts also ships `tpl-ask-default` ('ask' kind — the empty-send explain-my-screen QUESTION as a document, #130 posture; seeded seed-if-absent, read fresh per send over GET /templates/:id, edited over PUT with no rebuild)
│  ├─ screen/                   P4B  screen understanding (BUILT): processor.ts (subscribes capture.received, gated screen.ocr; source:'screen' image frames → invokeOcr → OcrResult + a distillate; skips the companion meta chunk; own last-failures ring; P4A×P4B: `runOnDrain(chunks,step)` is the DRAIN-stage entry — recognizes via the step's slot ocr→invokeOcr/vlm→invokeVlm, shares the build+persist helper, PROPAGATES throws for re-queue; #102 keep-time: stamps OcrResult.capturedAt from chunk.capturedAt so the record carries true capture time, not just the mirror Distillate window; **#174 slice C:** reports closed metadata-only `blank`/`failed` outcomes to the live tracker while successful nonblank recognition remains driven only by `ocr.completed`; legacy ingest reports then swallows OCR failures, workflow drain reports then rethrows the original error, and an outcome-reporter fault changes neither path) · router.ts (GET /screen/results · /screen/status) · registry.ts (WeakMap store→processor bridge, so both the http.ts-mounted router AND the executor's recognizeScreen seam reach the one processor) · index.ts (wireScreenOcr — main.ts wires it; the ingest subscription DEFERS while workflow.enabled is ON so screen understanding has ONE owner). **#175 deterministic acceptance:** `acceptance-175-e2e.test.ts` drives authenticated valid-JFIF bytes through both owners and distinct slots — legacy OCR, then workflow VLM — and asserts exact `OcrResult`/mirror-Distillate attribution + provenance, status/results/surface agreement, one-owner persistence, and ambient Ask context without a per-send image. The ocr/vlm INVOCATION lives in fabric/invoke.ts with the other slots; this is the processor+router only
│  ├─ voice/                    P2   resolve · interpolate · documents/defaults (registers+bindings) │ P5: comparator · chains
│  ├─ index/                    P2   extract (entities) · rank (recency×frequency) · relevant (relevant-now join) │ **#72 (BUILT):** phonetic.ts (pure, zero-dep: levenshtein/editSimilarity · compact Double-Metaphone primary+secondary · phoneticEqual · nameSimilarity — token soft-Jaccard-led blend, whole-string only when token counts differ) + resolve.ts (`resolveEntity` — the scored match step `score = phoneticFuzzy × corpusPrior × crossSourceCorroboration × personAffinity`, clamped; bands ≥0.85 auto/silent · 0.5–0.85 provisional · <0.5 new; a rival within ambiguityMargin 0.05 marks ambiguous; corpusPrior only-boosts + bounded; crossSource/personAffinity are neutral-1.0 INPUT MULTIPLIERS with no producer yet; honors rejectedRivalId. **#94 hardening:** corpusPrior/clamp01/sort are all finite-guarded — a corrupt `lastSeen` (NaN) can no longer win the sort or persist a NaN confidence; crossSource/personAffinity CLAMPED to `[0.5,1.5]` at the boundary so a future producer can't override the phonetic evidence; docstrings corrected to the TRUE guarantee — the boost only multiplies up (never drags an exact match below auto) but a multiplicative boost CAN lift a partial across a band edge, so the "never crosses a band alone" overclaim was fixed, not the code). Wired into `store/workspaces.ts` `upsertEntity` (override short-circuit runs FIRST; fuzzy link writes the corrupted form to heardAs not aliases; every resolution appends an `EntityResolution` to provenance; provisional/ambiguous stamps the #66 state+confidence+ambiguity, clean auto/first-create stay silent; **#94** a `new`-band create is stamped provisional only when its best same-kind rival lands NEAR the provisional band [`bestRivalScore ≥ provisionalBand − CREATE_PROVISIONAL_MARGIN`, 0.1] — an unrelated create [rival ≈0, e.g. a CJK name vs a Latin corpus] stays silent instead of being marked merely because a same-kind record existed). **#74 (BUILT):** correlate.ts — the crossSource PRODUCER (pure windowed correlator: `overlapsWindow` [OCR capture instant inside the heard window ± slack, NaN-safe] · `ocrForms`/`ocrTextForms` [separator AND path-component splitting, so "acme/pi.dev" yields "pi.dev"] · `correlate`/`correlateWindow` [max nameSimilarity of heard×OCR forms → `crossSourceCorroboration` multiplier at the resolver's clamp ceiling + a typed `seen` Sighting]); wired at the DISTILLER upsert seam (distiller.ts reads store.listOcrResults once per window → passes signals+crossSighting into upsertEntity — consumes the PERSISTED OcrResult stream, drain-timing-independent by construction, #115-safe); store side: a corroborated resolver LINK is stamped `confirmed` (the `linked && corroborated` branch in stampResolution — NOT a resolver band; corroboration promotes a genuine link, never a bare create) + the heard form taught to heardAs with both sightings recorded, idempotent on replay — heard+seen-same-window is the silent auto-teach path, zero contract change (entity v2 already carried Sighting/heardAs/signals). moveSession stays exact `findEntity` │ **#176 slice 1 (BUILT):** packets.ts — the deterministic ContextPacket builder (`buildContextPackets`, PURE like every index/ module: no DB/model/clock beyond inputs + injectable `now`): epoch-aligned `windowMs` buckets (default 60s) over stored SttSegments (mic + system-audio, lanes never merged) and OcrResults (screen) by TRUE capture instant (NaN ⇒ skipped, never guessed); focus evidence = the Session's window/repo attribution entries; candidates = in-window moments' entity refs, each checked against in-window screen forms with the #74 `correlate`/`ocrForms` (a match records ocrId+form+similarity — evidence with provenance); missing lanes degrade to `gaps` with machine-readable reasons; confidence = fixed lane-count map (0.4/0.7/0.9); idempotence + append-only supersession decided against the existing chain head by canonical-content equality (identical ⇒ no-op; different ⇒ revision+1 with `supersedes`, prior never mutated); ids content-derived (sha-256 over canonical JSON + chain position) so replay is byte-stable — proven over the #32 fixture in packets-replay.test.ts │ **#176 slice 2 (BUILT):** produce-packets.ts — the LIVE producer (the one index/ module beside relevant.ts that touches store): `materializeContextPackets` reads a session's stored observations, runs `buildContextPackets`, persists ONLY the appends, and records the attempt on an in-memory `PacketBuildLog` (the diagnostics "last update" signal) — CONTAINED (never throws: a read/build/write failure is caught + recorded, so a packet build can never sink the capture/distill path), idempotent, batched ONCE per session over the whole session, scoped to the session's OWN workspace. Wired at the `session.ended` seam in api/http.ts (after the drain/flush, over the fully-drained session) so packets materialize during normal capture with NO route call; the on-demand POST `/context/packets/build` now runs the SAME seam (unified build + log, still 404-validated). Proven served in `api/context-packets-e2e.test.ts` (real engine + fake stt/llm → /capture/mic → session end → the SERVED /settings/context-packets shows the converged window, NO build route called) + seam unit `index/produce-packets.test.ts` (idempotence · workspace isolation · contained failure) │ P4D (BUILT): canon.ts (mergeCanon — pure reference merging: same-kind duplicates whose name∪alias keys intersect fold into one canonical record via union-find, canonical winner = outboundCount→mentions→firstSeen→id; unions aliases/momentRefs/provenance, sums mentions+outboundCount, sets canonicalOf; relevant.ts folds through it before ranking) · rank.ts canon weight (× (1+outboundBoost·log2(1+outboundCount)) — "sent outranks viewed", log-damped, outboundCount 0 leaves score unchanged; write-side deferred — nothing increments outboundCount yet) · ingest/ (BUILT: chunk.ts page-anchored chunking [pure, deterministic ids] · fetcher.ts file[form-feed pages]/url[pageless]/pdf[honest stub]/gdoc[seam behind ingest.gdoc] · ingest.ts lifecycle → store pins/pin_chunks). Store: pins + pin_chunks tables (workspace-level canon, not session-keyed → not in moveSession). P4-T2 (BUILT): the ingest lifecycle over HTTP in api/http.ts — GET/POST `/pins?workspace=` · POST `/pins/:id/ingest` (runs ingestPin, honest failure = 200 with ingest.status:'failed', unknown pin 404) · GET `/pins/:id/chunks` (page-anchored excerpts, the "cite p.42" read) — ZERO logic change, routes over the store/ingest seams
│  ├─ act/                      P2   the Act primitive: draft (follow-up-draft on session end) · defaults/documents (act templates) │ P4A-s4 (BUILT): todo.ts — task-extract (composeTaskExtract + TaskExtractor rides the drain), TodoDocuments (todo-list docs), mergeTodoItems (normalized-text dedupe), renderTodo ({{todo}}); draft.ts interpolates {{todo}} │ P4+: nudge · exports
│  ├─ route/                    P3   detector · attribute · identity · reroute (BUILT: detector.ts — pure sustain-window context-switch detection over TimedSignals [focus+calendar]+hints, ONE window; attribute.ts — the Attributor auto-starts/switches sessions behind route.detect over the shared TimedSignal buffer, emits session.switched; hints.ts — workspace-hints docs (HintsDocuments all/get/put; P4-T3b: exposed over HTTP as GET `/hints` [= hintsDocs.all(), the detector's hint provider], GET/PUT `/hints/:workspaceId` in api/http.ts — the APPLY-with-review half of the teach loop, a plain versioned-document edit, unknown-workspace PUT CREATES like PUT /workflows, ZERO route/ logic change); focus.ts — focus-chunk decode; reroute.ts — one-click retroactive session move + session.rerouted, the correction/teaching signal. identity.ts is P7) │ P4C calendar evidence (BUILT): calendar.ts — pure decode of a raw Calendar.app sample → TimedCalendarSignals; calendar-collector.ts — CalendarPoller (engine-side osascript/JXA poll of the current/imminent event, per-tick route.detect gate, graceful degrade) + startCalendarCollector(app,…) mounted from main.ts like wireScreenOcr, feeding the ONE Attributor (exposed on EngineApp). Hint fields eventTitle/attendee → 'calendar' evidence. Gated on route.detect (no new flag); topic-drift out of scope
│  ├─ ledger/                   P4   commitments · watchers/{repo,doc,mail} · prepare (action cards — builds on act/)
│  ├─ queue/                    P1   spool · drain (**#116:** `stop()` — the shutdown seam close() calls before the store closes: no new drain starts, the in-flight one exits at its next file boundary, spooled files stay durable — kills the drain-after-close teardown race behind the known mid-sequence test flake; P2: optional distill processor · drainNow flushes before the act; #58: drains until the spool is EMPTY — loops, stopping on a failure to keep retry-at-idle — so the distill cadence throttle's material reliably accumulates across drains; #70: FRESHNESS-FIRST order by fs mtime — newest-first while a session is live (render the present), oldest-first at idle (FIFO the backlog) via an injected isSessionLive probe — plus an age-shed policy: backlog older than OPENINFO_QUEUE_MAX_AGE_MINUTES (default 10m) is DROPPED not processed, never re-queued, with a log line (count + age range) + QueueStatus.shedFiles counter) │ P4A-s3: kinds.ts (audio/screen/llm-work classify, focus excluded) · eta.ts (backlog projection, honest unknowns) · GET /queue byKind/eta/overflow/shedFiles (additive); #102 keep-time: status() also computes QueueStatus.lag (BacklogLag — now − oldest-pending WORK-chunk capturedAt; absent when caught up; basis capture-time|unknown) │ **#115 (BUILT): the drain is TWO independent tracks** — a SECOND CaptureQueue at `queue-text/` carries transcribed utf8 CaptureChunks (a transcribed segment IS a valid CaptureChunk, so the text stream reuses ALL spool machinery — per-track single-flight, freshness-first, age-shed, overflow/ETA — with zero contract change). Audio track (api/http.ts): focus-observe + runTranscribe + #58 live fast-path + append segments to the text queue, NEVER awaits an LLM; text track: distillThrottled (distill/moments/fields/judge) on its own single-flight loop, DistillCadence unchanged — STT latency is independent of an in-flight distill (the cold-boot freeze class is structurally gone). `drainBoth` sequences audio-then-text at session-end flush; `surfacedQueueStatus` merges the most-recent lastFailure across tracks for GET /queue · Status · /senses (capture backlog stays the audio track's); the calendar collector's FIRST sample is gated on firstTranscriptSeen with a grace-window fallback (route/calendar-collector.ts — cold boot no longer pops Calendar.app mid-first-session; privacy gate still outranks readiness) │ gc
│  ├─ overlay/                  P2   rules/lenses · roles · ontology (voice lives in voice/)
│  ├─ flags/                    P0   flag registry (flags are documents; read.ts::isFlagEnabled, read per-drain/event so gates like workflow.enabled / act.tasks are hot-flippable)
│  ├─ surfaces/                 P2   HUD surface documents (documents/defaults; **#100** defaults.ts also seeds `surf-openinfo-fields` — the FIELDS-PANEL APP (now · one always-visible `fields` block [top 8, all three seeded fast-field prompts as rows, full glyph verb set] · the `distillates` stream; template mirrored at templates/openinfo-fields/surface.json; the #58 live-transcript strip is controller-composed, not a stack block), seeded via the `SEEDED_SURFACES` list ensureDefaults walks (seeds-if-absent so user edits never clobber) and list() defensively unshifts; **#101** defaults.ts also seeds `surf-openinfo-diagnostics` — the DIAGNOSTICS APP (transcription inspector + sense-gates + queue/lag blocks, framed `app` chrome); **#133** defaults.ts also seeds `surf-openinfo-notetaker` — the meeting NOTE-TAKER APP, the mainstream look-and-speed EXEMPLAR (defined in its OWN module `surfaces/notetaker.ts`, imported by defaults.ts; template mirrored at templates/openinfo-notetaker/surface.json): a three-zone Meetily-shape layout composed from EXISTING blocks/sources ONLY — LEFT `pinned-doc`(pins)+rail chrome, CENTER `now`/`moments`/`distillates` canvas, RIGHT `distillates`(the rolling summary)+`todos`+`fields`; the three columns are DOCUMENT-EXPRESSED — each block names its zone via an `nt-left-`/`nt-center-`/`nt-right-` id PREFIX that the client `hud/notetaker-layout.ts` partitions the flat stack by and renders per-zone through the SAME `renderSurface` (no new block type, no contract change, the generic renderer untouched); framed `app` chrome at width 960; list() + GET /layouts/surfaces; **the-pill** defaults.ts also seeds `surf-openinfo-pill` (own module `surfaces/pill.ts`) — THE PILL, the MVP Standard App glance face registered as the bundle's `hud` face; the Listen glance stack + below-panel geometry (bar 56 / ask 432), client renderer `hud/pill-layout.ts` + height authority `hud/pill.ts` (see the-pill slice); **#174 slice D** `surfaces/pill.ts` ships `defaultPillSurface` **v2** — the composable `sense-lanes` organ inserted after `now` so live capture truth precedes recalled content (former `moments` block relaxed to `on-match`), the pre-organ body kept as `LEGACY_DEFAULT_PILL_SURFACE` + serialized `PREVIOUS_DEFAULT_PILL_BODY`; `documents.ts` `ensureDefaults` runs ONE conservative seed refresh — a stored pill advances to v2 ONLY when its LayoutStore record is version 1 AND byte-identical to the shipped v1 body, so any user save (record-version bump) or edit (body change) is never clobbered, and the refresh is itself idempotent (becomes record v2 carrying body v2); **#99** `POST /layouts/surfaces/:id/instantiate` in api/http.ts deep-clones a template into a FRESH surface — new id (`surf-<uuid>` if unspecified) + bound workspace (`ws-<slug>`, ensureWorkspace'd) + version 1, contract-validated, 404 unknown source / 409 id collision, publishes surface.updated; instances list automatically so the tray Apps folder shows them, no client change) · block-query compiler (query.ts: BlockQuery→store calls; **#99** an optional `defaultWorkspaceId` (the instance binding, resolved by `POST /query?surface=<id>` from the surface's workspaceId) is the query's default workspace — precedence explicit `params.workspace` > instance binding > `'default'`, so one context-agnostic block reads each instance's own silo; sources relevant-now/moments/sessions/entities/**pins**/**todos**/**drafts**/**teach**/**distillates** hydrate through store/, unknown workspace ⇒ []; `todos` flattens the session/workspace to-do documents to items via store.listTodos (the `_meta.db` docs, not a workspace DB, so ungated); `drafts` reads store.listDrafts newest-first (workspace-DB records, `known`-gated); `teach` DERIVES SUGGESTED hint candidates via deriveHintCandidates over TeachStore.list (the read-only review half of the teach loop — the same derivation GET /teach/candidates serves; workspace-scoped, `_meta.db` docs so ungated, never auto-applied); `distillates` reads store.listDistillates newest-first (workspace-DB records, `known`-gated — the persisted distillate stream; raw pre-distill transcripts are transient with no persistence path, so the durable queryable stream is the distilled-window text); `fields` (**#61**) reads FieldValueStore.list(ws, session?) — the fast fields' latest values, one FieldValue row each with provenance (templateId·endpoint·model) + `state:'provisional'`, freshest first, session query also returns workspace-scoped fields, ungated (docs) ⇒ unknown ws []; `transcript` (**#101**) + `senses` (**#7/#101**) are injected operational/computed state — the transcript ring + current stt slot, and the evaluateSenseGates verdict (no live probe); **#174 slice D** adds the injected `live-senses` source — the process-local `SenseLaneTracker` snapshot set resolved by `POST /query?surface=<id>` (explicit params.workspace > instance binding > default; `session:'current'` or omitted = the tracker's current session, never a stale persisted Session; a concrete id the tracker never observed this launch ⇒ honest stopped/no-session rows), the SAME closed metadata-only rows GET /senses/live serves, wrapped and capped by `compileQuery` through the shared exported `resolveQueryScope` and proven by the authenticated `live-senses-query.test.ts`; **#210 honest display scope:** `resolveQueryScope` now binds `session:'current'` for the RECORD sources to the RUNTIME-current session — the SAME `SenseLaneTracker.currentSessionId` authority the live lanes already use, threaded from the /query route — NOT `store.liveSession`'s persisted most-recent-unended session; when no session is live this process (a fresh launch with a stale unended session on disk) the scope carries `noCurrentSession` (no sessionId) and the session-scoped sources (relevant-now/moments/todos/drafts/distillates/fields) read honest-EMPTY instead of silently widening to whole-workspace history (the daily-felt "lingering records from a previous session"); an all-history block with NO session param is unchanged; GET `/sessions?live` (`readSessions`) is runtime-gated the same way so the HUD Now line is never adopted from a stale persisted session (always-launch-stopped posture, one authority for both surfaces); proven by `query.test.ts` (resolver + per-source empties) and the driven `http.test.ts` #210 e2e (real /sessions + /query routes: stale session → empty; start-this-process → resolves; end → empty again); `queue` is the third non-store source — operational engine state (backlog/ETA/overflow/lastFailure) INJECTED via QuerySources by the /query route (awaits ctx.queue.status()), one QueueStatus row, absent ⇒ []; **#66** every id-bearing source (relevant-now via entity.id, moments/entities/pins/todos/drafts/distillates via id) drops rows the workspace DISMISSED — `ItemSignalStore.dismissedKeys`, keyed `${source}:${id}` — and discloses the suppressed COUNT on the result so an all-dismissed block explains itself; only `ledger` awaits its store P4) · signals.ts (**#66** ItemSignalStore — per-item dismiss/follow-up signals as ONE `_meta.db` document per workspace, LayoutStore-versioned like TodoDocuments; `add` idempotent per (source,itemId,kind); `dismiss` = the SUPPRESSION record query.ts honors, `follow-up` persisted+queryable in the same store; written over POST /item-signals) · settings/ (GET /settings — ENGINE-served SIDEBAR shell: registry.ts = one table of pure section modules {id,group,label,render,liveDot}; shell.ts renders grouped sidebar + active section; sections/ = Status·Features·Privacy·**Audit-ledger (#65)**·**Trace (#116)**·**Context packets (#176)**·Benchmarks; assets.ts = SaaS-grade CSS/script; **#65** `sections/ledger.ts` — pure `buildLedger(distillates,ocrResults)→newest-first passes` (llm distillate = distill pass, OcrResult = screen pass, ocr/vlm distillates skipped as mirrors) + `renderLedger` hop-trail table (when·stage·endpoint/model·tokens in/out+`est`·guard·egress); **#63** guard column lit from `provenance.guard` (clean·redacted·N·unguarded·"— no guard") + a held-hops block with release/deny (POST /guard-holds/resolve); `data.ledger` assembled in `api/http.ts` ONLY when the section is active — 2 cheap store reads, default workspace); **#176** `sections/packets.ts` — pure `buildContextPacketViews`/`renderContextPackets` render the converged-slice diagnostics: per-window membership (lane refs, screen text RESOLVED from the source OcrResult at render — refs never carry content), exclusions (the `gaps` reasons in human words), timing (window bounds + observation instants), lane-count confidence (framed; raw value on inspection), and the append-only supersession chain; the live producer's last outcome renders as the honest "last update" / "last update didn't finish" line; every state is TEXT (empty · assembly `problem` · contained build failure); `data.contextPackets` assembled in `api/http.ts` only when active — default workspace, superseded revisions included · setup/ (the re-homed section fns: view.ts pure fabric/profile/keys/get-started/try-it fragments; surface-editor.ts + editor-assets.ts — the HUD-LAYOUT editor at /settings/hud-layout?surface=<id>). /setup 301s to /settings │ P4: serve workbench │ P6: WYSIWYG/drag-drop editor · custom-block sandbox (rabbithole pattern)
│  ├─ surfaces/                 P2   HUD surface documents (documents/defaults; **#100** defaults.ts also seeds `surf-openinfo-fields` — the FIELDS-PANEL APP (now · one always-visible `fields` block [top 8, all three seeded fast-field prompts as rows, full glyph verb set] · the `distillates` stream; template mirrored at templates/openinfo-fields/surface.json; the #58 live-transcript strip is controller-composed, not a stack block), seeded via the `SEEDED_SURFACES` list ensureDefaults walks (seeds-if-absent so user edits never clobber) and list() defensively unshifts; **#101** defaults.ts also seeds `surf-openinfo-diagnostics` — the DIAGNOSTICS APP (transcription inspector + sense-gates + queue/lag blocks, framed `app` chrome); list() + GET /layouts/surfaces; **#99** `POST /layouts/surfaces/:id/instantiate` in api/http.ts deep-clones a template into a FRESH surface — new id (`surf-<uuid>` if unspecified) + bound workspace (`ws-<slug>`, ensureWorkspace'd) + version 1, contract-validated, 404 unknown source / 409 id collision, publishes surface.updated; instances list automatically so the tray Apps folder shows them, no client change) · block-query compiler (query.ts: BlockQuery→store calls; **#99** an optional `defaultWorkspaceId` (the instance binding, resolved by `POST /query?surface=<id>` from the surface's workspaceId) is the query's default workspace — precedence explicit `params.workspace` > instance binding > `'default'`, so one context-agnostic block reads each instance's own silo; sources relevant-now/moments/sessions/entities/**pins**/**todos**/**drafts**/**teach**/**distillates** hydrate through store/, unknown workspace ⇒ []; `todos` flattens the session/workspace to-do documents to items via store.listTodos (the `_meta.db` docs, not a workspace DB, so ungated); `drafts` reads store.listDrafts newest-first (workspace-DB records, `known`-gated); `teach` DERIVES SUGGESTED hint candidates via deriveHintCandidates over TeachStore.list (the read-only review half of the teach loop — the same derivation GET /teach/candidates serves; workspace-scoped, `_meta.db` docs so ungated, never auto-applied); `distillates` reads store.listDistillates newest-first (workspace-DB records, `known`-gated — the persisted distillate stream; raw pre-distill transcripts are transient with no persistence path, so the durable queryable stream is the distilled-window text); `fields` (**#61**) reads FieldValueStore.list(ws, session?) — the fast fields' latest values, one FieldValue row each with provenance (templateId·endpoint·model) + `state:'provisional'`, freshest first, session query also returns workspace-scoped fields, ungated (docs) ⇒ unknown ws []; `transcript` (**#101**) + `senses` (**#7/#101**) are injected operational/computed state — the transcript ring + current stt slot, and the evaluateSenseGates verdict (no live probe); **#174 slice D** adds the injected `live-senses` source — the process-local `SenseLaneTracker` snapshot set resolved by `POST /query?surface=<id>` (explicit params.workspace > instance binding > default; `session:'current'` or omitted = the tracker's current session, never a stale persisted Session; a concrete id the tracker never observed this launch ⇒ honest stopped/no-session rows), the SAME closed metadata-only rows GET /senses/live serves, wrapped and capped by `compileQuery` through the shared exported `resolveQueryScope` and proven by the authenticated `live-senses-query.test.ts`; **#210 honest display scope:** `resolveQueryScope` now binds `session:'current'` for the RECORD sources to the RUNTIME-current session — the SAME `SenseLaneTracker.currentSessionId` authority the live lanes already use, threaded from the /query route — NOT `store.liveSession`'s persisted most-recent-unended session; when no session is live this process (a fresh launch with a stale unended session on disk) the scope carries `noCurrentSession` (no sessionId) and the session-scoped sources (relevant-now/moments/todos/drafts/distillates/fields) read honest-EMPTY instead of silently widening to whole-workspace history (the daily-felt "lingering records from a previous session"); an all-history block with NO session param is unchanged; GET `/sessions?live` (`readSessions`) is runtime-gated the same way so the HUD Now line is never adopted from a stale persisted session (always-launch-stopped posture, one authority for both surfaces); proven by `query.test.ts` (resolver + per-source empties) and the driven `http.test.ts` #210 e2e (real /sessions + /query routes: stale session → empty; start-this-process → resolves; end → empty again); `queue` is the third non-store source — operational engine state (backlog/ETA/overflow/lastFailure) INJECTED via QuerySources by the /query route (awaits ctx.queue.status()), one QueueStatus row, absent ⇒ []; **#66** every id-bearing source (relevant-now via entity.id, moments/entities/pins/todos/drafts/distillates via id) drops rows the workspace DISMISSED — `ItemSignalStore.dismissedKeys`, keyed `${source}:${id}` — and discloses the suppressed COUNT on the result so an all-dismissed block explains itself; only `ledger` awaits its store P4) · signals.ts (**#66** ItemSignalStore — per-item dismiss/follow-up signals as ONE `_meta.db` document per workspace, LayoutStore-versioned like TodoDocuments; `add` idempotent per (source,itemId,kind); `dismiss` = the SUPPRESSION record query.ts honors, `follow-up` persisted+queryable in the same store; written over POST /item-signals) · settings/ (GET /settings — ENGINE-served SIDEBAR shell: registry.ts = one table of pure section modules {id,group,label,render,liveDot}; shell.ts renders grouped sidebar + active section; sections/ = Status·Features·Privacy·**Audit-ledger (#65)**·**Trace (#116)**·**Context packets (#176)**·Benchmarks; assets.ts = SaaS-grade CSS/script; **#65** `sections/ledger.ts` — pure `buildLedger(distillates,ocrResults)→newest-first passes` (llm distillate = distill pass, OcrResult = screen pass, ocr/vlm distillates skipped as mirrors) + `renderLedger` hop-trail table (when·stage·endpoint/model·tokens in/out+`est`·guard·egress); **#63** guard column lit from `provenance.guard` (clean·redacted·N·unguarded·"— no guard") + a held-hops block with release/deny (POST /guard-holds/resolve); `data.ledger` assembled in `api/http.ts` ONLY when the section is active — 2 cheap store reads, default workspace); **#176** `sections/packets.ts` — pure `buildContextPacketViews`/`renderContextPackets` render the converged-slice diagnostics: per-window membership (lane refs, screen text RESOLVED from the source OcrResult at render — refs never carry content), exclusions (the `gaps` reasons in human words), timing (window bounds + observation instants), lane-count confidence (framed; raw value on inspection), and the append-only supersession chain; the live producer's last outcome renders as the honest "last update" / "last update didn't finish" line; every state is TEXT (empty · assembly `problem` · contained build failure); `data.contextPackets` assembled in `api/http.ts` only when active — default workspace, superseded revisions included · setup/ (the re-homed section fns: view.ts pure fabric/profile/keys/get-started/try-it fragments; surface-editor.ts + editor-assets.ts — the HUD-LAYOUT editor at /settings/hud-layout?surface=<id>). /setup 301s to /settings │ P4: serve workbench │ P6: WYSIWYG/drag-drop editor · custom-block sandbox (rabbithole pattern) · **#134** panel-surfaces.ts also seeds `surf-openinfo-chat` (below-HUD chat: now + an `input` block wired to POST /chat, panel edge:below collapsed 120→expanded 432) and `surf-openinfo-sidebar` (collapsible right sidebar: now + relevant-now cheat-sheet, panel edge:right collapsed 0→expanded 320, reveal:event openOn `entity.updated`) — kept in their OWN module so SEEDED_SURFACES adds one import + one spread
│  └─ teach/                    P4D  quality flywheel (BUILT, capture side): signals.ts — TeachStore (store-backed `teach-signals` document keyed by corrected-TO workspace, HintsDocuments pattern) captures every `session.rerouted` correction as a labeled TeachSignal; captureReroute (Session→TeachSignal, pure); deriveHintCandidates (pure: accumulated reroutes → SUGGESTED AttributionPattern candidates for the corrected-to workspace, NEVER auto-applied to route/hints). index.ts — wireTeach(app) mirrors wireScreenOcr (one line in main.ts, no route, no flag). P4-T2 (BUILT): GET `/teach/candidates?workspace=` in api/http.ts serves deriveHintCandidates over TeachStore.list — SUGGESTED, inspectable, citable chips; READ-ONLY, never applied to route/hints. Contract: HintCandidate (payloads.ts). P4-T3b (BUILT): the APPLY half is GET/PUT `/hints[/:workspaceId]` (route/hints.ts store seam over HTTP) — the user reviews a candidate and PUTs its pattern into the workspace's WorkspaceHints; no auto-apply verb, just a document edit. **#75 (BUILT):** `TeachSignalKind` gained the entity-correction kinds (alias-confirm/alias-reject/rename/disambiguate) + dismiss (append-only union); `TeachSignal`'s reroute fields are now optional + an optional `entity` (EntityCorrectionSignal) variant flows through the SAME TeachStore (entity corrections file under their entity workspace); `captureEntityCorrection` (pure) builds the labeled signal; `POST /teach/entity` writes it alongside the sovereign override + clears ambiguity. alias-reject/rename/dismiss are declared-not-emitted (no create-new-entity/rename/dismiss surface yet). Feeding candidates back into extraction prompts is deferred
│
├─ apps/client/src/             thin Electron client — NEVER opens a database
│  ├─ capture/                  P1   mic (BUILT: hidden-window getUserMedia → webm segments → /capture/mic, session-gated; **#95 chunking architecture:** vad.ts holds the PURE pause-based rotation decision (shouldRotate/nextSilenceRunMs/resolveVadParams) and the renderer feeds it AnalyserNode amplitude each poll tick — it rotates the MediaRecorder at a detected PAUSE, not the wall clock, so a cut never splits a word (measured whole-file-equal ~0.00 WER vs ~0.20 at the old fixed-1s in tools/stt-accuracy); `fixed` cadence still selectable. ChunkStrategy + vad knobs resolve in main/config.ts (env>file>default, default `vad`) and ride CaptureStartOptions; #41: the renderer pings `capture:loaded` on module load and acks each start via `capture:start-ack` — see main/capture-dispatcher) · capture-preload (#41 FIX: was importing channel constants from the ESM sibling ./protocol.js, which a sandboxed preload cannot require — so the WHOLE preload failed to load, `window.openinfoCapture` was undefined, and every chunk was silently unsent; now inlines the channel strings like the HUD preload, protocol.ts stays the typed source of truth, guarded by a compiled-.cjs drift test) · capture-controller (#41: un-wedgeable — a stop-ack timeout force-clears a stuck `stopping` and drains `pendingStart`; `onStartFailed` drops a stuck `starting` to idle when the dispatcher gives up) · system-audio (BUILT: 2nd stream in the SAME hidden window → /capture/system-audio="them"; **#142** `ShellConfig.systemAudioMethod` picks HOW it opens — `loopback` (macOS `auto` default: Chromium CoreAudio-Tap via getDisplayMedia, NO device/routing, rides the Screen-&-System-Audio-Recording TCC + NSAudioCaptureUsageDescription plist) or `device` (BlackHole detect-and-guide fallback, 2nd getUserMedia on a matched virtual input); both feed the identical chunk/VAD/silence pipeline. See ARCHITECTURE §8) · screen (P4B, BUILT: main-process desktopCapturer polled at cfg.screenIntervalMs, opt-in OPENINFO_SCREEN/cfg.screenEnabled; each frame → an image chunk + a companion ScreenFrameMeta chunk, both scr- prefixed; **PR #152 Δ-gate (first pass on #5):** capture/frame-delta.ts holds the PURE gate — computeDeltaScore (sampled byte-diff over a 32px probe, per-byte tolerance 8, fail-open 1 on length-mismatch/empty) + shouldSend (threshold 0.015 OR every-10th-tick heartbeat) + per-displayId `FrameDeltaGate` scoring against the last KEPT probe so gradual drift accumulates; shell.ts derives the probe pre-JPEG, early-outs gated frames, stamps meta.deltaScore on kept ones, resets the gate per session start; `OPENINFO_SCREEN_DELTA`/cfg.screenDeltaThreshold, 0 = gate off; aec deferred; #4: the cadence loop is capture/screen-source.ts `startScreenCadence` — grab-now-then-on-interval, fake-timer unit-tested — and cfg.screenIntervalMs is CLAMPED into the owner's 3–6s target band; **#174 slice C:** `screen-observation.ts` wraps each serialized tick with one attempt id/time and emits closed `queued`/`delta-skipped`/`grab-failed` metadata; queued provenance comes only from CaptureController's exact durable image receipt, its expected-context guard prevents run-A pixels being retagged as run B, failed durability resets the delta baseline, and `EngineLink.observeScreen` is authenticated/one-refresh, never spooled, and bounded replace-latest (a newer report aborts its predecessor; a 5s timeout aborts the final hung request without stopping sensing); **#192:** `capture-controller.ts` gains the optional `onDenied` seam — fired with the refused run's EXACT session context on a refused/thrown permission request or a renderer mid-run `permission-denied` status (no run ⇒ no report) — and `screen-observation.ts` `screenPermissionDeniedObservation` builds the closed metadata-only report the shell wires to `EngineLink.observeScreen` on the screen controller, so an OS-refused screen lane reads blocked/permission-denied instead of idle) │ P2: calendar │ focus (P3, BUILT: main-process osascript poll of the frontmost app/window → redacted FocusSignal → /capture/focus, session-INDEPENDENT, gated on route.detect flag + OPENINFO_FOCUS opt-out; ephemeral, never spooled) │ P7: camera
│  ├─ engine-link/              P1   typed client from contracts · offline spool · #123 local EngineAuthDiscovery validates owner-only per-port records; HTTPS tunnel clients use explicit env/owner-only token-file credentials; Electron main attaches Bearer only to the exact engine origin and pinned trusted webContents, renderer JS never sees it, WS reloads on reconnect; plain-browser dev needs an explicit browser-session bootstrap
│  ├─ main/                     P1   the Electron shell (BUILT): shell.ts (electron entry; wired-up-arc P4: a `hud:resize` IPC handler content-sizes the HUD via `resolveHudHeight` → `setContentSize`, top-anchored so drag/position persist; **#19/#20/#98 mini-apps-in-a-folder**: the drag/resize IPC is now routed by `event.sender` to the exact window that sent it, so N surface windows share one shell — `createSurfaceWindow(surfaceId,{chrome,isDefaultHud})` is the generalized factory the default HUD and every Apps-folder window share — **#194** its constructor BODY is extracted to `surface-window.ts` (`constructSurfaceWindow(surfaceId, opts, env)`, the one other electron-importing main module) so the driven pill e2e invokes the SAME production function; shell.ts's factory is now the thin wrapper binding the shell-state seams (`SurfaceWindowEnv`: engine URL/outline, `pinAuth` engine-credential pinning, `registerMeta` window meta, position-store restore/save) —, `openApp/closeApp/toggleFavorite` dispatch the parameterized app commands, persisted open-set reopens at boot, per-app positions restore) · app-registry (**#19** pure generic `WindowRegistry<W>` — windows keyed by surface id, open-or-focus / close / retire-on-closed / openSurfaceIds; DI'd create/focus/close/isAlive so it's headless-tested with fake windows; a window is BORN bound to its surface, multi-window is a REGISTRY not a re-binding) · app-catalog (**#98** pure Apps-folder builder — `sortApps` floats favorites to top then alphabetizes, `appsSubmenuItems` renders one submenu per app [● open / ★ favorite markers] carrying open-app/close-app/toggle-favorite commands; **bundle-as-runtime-object**: a bundle [GET /bundles] renders as ONE parent app whose faces open the mapped surfaces [open-app/close-app naming the face surfaceRef, face label = mapped surface name], and any surface NOT claimed by a bundle face DEMOTES to a standalone catalog row — the owner IA "Apps > Standard App > HUD/Chat/Support faces") · app-store (**#19/#20/#98** client-local `apps-state.json` — favorites + open-set + per-surface window positions; pure parse/serialize tolerant like parseClientConfigFile, separate from the default HUD's own window-state.json) · window-options (content-protection, always-on-top, frameless; wired-up-arc P4: the HUD is CONTENT-sized — opens at `HUD_MIN_HEIGHT`=144 and grows/shrinks to the painted panel, no fixed 708×720 dead zone; **#20** per-surface chrome — `configForSurface`/`SURFACE_WINDOW_CONFIG` map the shipped HUD surfaces to the transparent Glass shell [width overridable, default preserved] and any OTHER surface to `appWindowSpec` = a normal framed/opaque/resizable app window [disclosed default for unknown ids]; **#100** `surf-openinfo-fields` → Glass chrome at width 480, the content-protected companion beside the HUD; **#133** `surf-openinfo-notetaker` → framed `app` chrome at width 960, the three-zone note-taker workspace you WORK in + screenshot for the look-and-speed review) · hud-height (wired-up-arc P4: pure `resolveHudHeight` — non-finite→floor, fractional ceiled, capped at display work-area, floored at HUD_MIN_HEIGHT; headless-tested) · tray-menu (show/hide · start/end session · live indicator · permission fix-it items; **#98** an "Apps" folder submenu of app surfaces [GET /layouts/surfaces], favorites-first, open/focus/close + favorite per app; folder now also shows when only bundles are known — bundle-as-runtime-object) · shortcuts (⌘\ hide · Settings deep-links; **#19/#98** `ShellCommand` widened from a parameterless union to also carry the parameterized `AppCommand` = {kind:'open-app'|'close-app'|'toggle-favorite', surfaceId}) · engine-session (session client + WS live-state) · engine-supervisor (0.0.1: adopt-or-spawn decision + /health probe + wait-for-engine — PURE, tested headless; shell.ts wires utilityProcess.fork of the bundled engine when nothing answers, adopt-not-collide, kills only its own child on quit; wired-up-arc P1: `fetchEngineHealth` + pure `compareVersions`/`engineStatusLine` capture the adopted/spawned engine's `/health` version → the tray shows `engine v0.0.1 · adopted at :8787` / `· spawned (bundled)` with a plain skew note when an adopted engine differs from this app) · config (env > ~/.openinfo/client.json > defaults; #4: SENSE_DEFAULTS pins the senses-on defaults — mic/system-audio/focus ON, screen opt-in OFF — and resolveScreenIntervalMs clamps the screen cadence into the 3–6s band) · tray-icon · permission-help (Settings deep-links + LAN classify) · context-health (Accessibility-hint tracker) · first-run (open /settings once; wired-up-arc P2: `shouldPromptMic` + a once-only `micPromptedAt` marker fire a PROACTIVE mic TCC popup at first launch, before any /settings open) + first-run-store · capture-status (wired-up-arc P2: pure map of macOS TCC statuses + system-audio device presence → an honest per-sense readout the tray renders as a "Capture status" submenu — mic/screen/system-audio each with state + honest copy incl. the screen "flip then relaunch" reality + one-click Settings-pane links; no IPC bridge — the tray builds in-process; #7: `captureStatuses` now composes the FIRST blocking gate per sense end-to-end — sense-off → os-permission → engine-unreachable → no-session → the engine-side gate fetched from `GET /senses` via `EngineSessionClient.senses()` — so no sense reads as a bare "off"; the tray submenu names the deeper gate + its fix, #41 lines intact) · capture-consent (#41: pure boot guard — capture only auto-starts on a live-session transition the USER started THIS launch; a leftover session seeded at boot opens STOPPED, never silently resumed; grant on tray Start, revoke on End/quit, persists across auto-end→restart) · capture-dispatcher (#41: pure renderer readiness + start-ack handshake — QUEUES a start until the renderer pings `capture:loaded`, then SENDS + awaits `capture:start-ack`, RESENDS on timeout up to a cap, else a VISIBLE tray fault; kills the fire-and-forget `webContents.send` that raced boot and was silently dropped; mic/system-audio route `control.start/stop` through it, screen stays main-process) · client-log (#41: dependency-free rotating log at `<userData>/logs/client.log`→`.1` at a size cap, never throws — the packaged app has no terminal, so capture lifecycle + faults had gone to a lost stdout) · shell.ts also: ends the live session on `before-quit` (bounded, so it never outlives the client), gates `applyCaptureLifecycle` on consent, surfaces a `captureFault` on the tray, and reloads the capture window on `render-process-gone`/`did-fail-load` (#41) · scripts/package.mjs (→ ad-hoc-signed release/openinfo.app WITH a bundled engine-bundle/ extraResource; `pnpm package`) · scripts/dmg.mjs (→ release/openinfo-<version>-arm64.dmg via hdiutil, /Applications symlink; `pnpm dmg`) · scripts/hud-bounds-e2e.mjs (wired-up-arc P4: driven REAL-Electron e2e asserting the content-sized HUD's real window bounds track content — empty 152→grown 398→shrunk 152; a "probe main" needing a GUI; `pnpm test:e2e:hud`, not in the headless default `test`) · scripts/multiwindow-e2e.mjs (**#19** driven REAL-Electron e2e over the REAL WindowRegistry + hud.html: opens TWO surfaces → two live windows each render their OWN `?surface=` document → closes one → the other stays alive with its panel intact; `pnpm test:e2e:multiwindow`, GUI-only) · scripts/capture-lifecycle-e2e.mjs (#41: driven REAL-Electron e2e over the REAL preload+renderer+dispatcher — proves the `capture:loaded` readiness ping, the boot guard (live session + no consent starts nothing), a consented start delivered+acked+`ready`, and a sabotaged renderer's dropped start surfacing a VISIBLE fault instead of wedging; `pnpm test:e2e:capture`, GUI-only) · **Ask face:** preload.cts adds the `openinfoScreen.captureFrame` bridge (ipc invoke `hud:capture-frame`) beside the S2 attach bridge; shell.ts factors `grabPrimaryDisplayImage` out of the cadence grab and shares it with `captureAskFrame` — ONE Δ-gate-free still per EXPLICIT chat send, consent gated in main (cfg.screenEnabled opt-in AND the OS grant; refusal answers an honest {ok:false,reason}, never a silent null, never a blocked send; no capture outside a send) · scripts/ask-face-e2e.mjs (driven REAL-Electron e2e over the served chat window: real-typed send ships one frame + turnId, the provisional streamed turn paints BEFORE the HTTP reply then reconciles, an EMPTY send posts the tpl-ask-default document body, the thread rehydrates across a window destroy+reopen, a refused-capture empty send is a visible no-op with NO post; frame source injected at the ipcMain seam — TCC can't grant a throwaway harness; `pnpm test:e2e:ask`, GUI-only)
│  └─ surfaces/                 P2   block-renderer (pure VNode, render(surfaceDoc); `wireActions` (GATES on the exported `WIRED_VERBS` source-of-truth set — a `data-verb` not in it is ignored before the dispatch branches) delegates the WIRED verbs to injected `ActionHandlers` and paints each awaited outcome onto the clicked button — transient label+class, never a silent no-op: `copy`→`CopyFn` (`Copied`/`Copy failed` #43), `mark-done`→`markDone` PUT /todos read-flip-write (`Done`/`Failed`), `accept`→`accept` PUT /hints append-pattern (`Accepted`/`Failed`) #15; `dismiss`→`dismiss` POST /item-signals suppression (`✓`/`!`) #66; **#96** `mute-system-stream`→`muteSystemStream` — a client-local DISPLAY toggle (NOT a write, so NOT through paintFeedback: the re-render it triggers repaints the button in its new hide↔show state, which IS the feedback); **#75** `clarify-confirm`/`clarify-rival`→`clarify` POST /teach/entity (the ambiguous-mention verdict; paints ✓/! on the choice) while `clarify-open`/`clarify-dismiss`→`clarifyOpen`/`clarifyDismiss` are client-local Hud session flips (expand / ask-me-later, no write); a verb whose handler is absent or whose button carries no payload stays visible-but-inert, as do the still-unwired open/navigate/pin/mark-for-follow-up/run-mode/draft-with) · micro-state.ts (**#66** the field micro-state DOT — dot-scale, colour-coded judge tier `stateDot(state, vocab)`; renders ONLY when an item carries a `state`, so nothing pretends to be reviewed (no judge stamps one yet); vocab is provisional/confirmed/corrected by default, `block.states` REPLACES it for a document-defined vocabulary) · blocks/ (glyph verb strip **#66**: `glyphStrip`/`rowAffordances` render dismiss/pin/mark-for-follow-up as a compact ~60px three-glyph strip partitioned from the text `.mini` verbs — dismiss LIVE with its {workspace,source,item} payload, pin/follow-up honestly inert ghosts; the verb SET is `block.actions`, shipped defaults on relevant-now; todos rows also render the micro-state dot before the title (its `state` field, absent today) and disclose "N dismissed" when emptied purely by suppression #66; built-ins + glyphs; relevant-now renders the recency×frequency join from `source:'relevant-now'` — one row per RelevantEntity: entity glyph + a WHY line that PREFERS the RECORDED provenance trail and FALLS BACK to the mention-count + latest-moment heuristic, dropping any row that can state no why sentence (display rule #1) #14; **#117** as a HUD-tier surface its why states only the HUMAN slice — source kind (heard / on screen / from calendar, from the entity's latest `sighting`, else the honest `heard` default) + recency clock (provenance window end, else `lastSeen`), NEVER the endpoint, model id, or template id (the full machine trail stays recorded on the entity and reachable on the diagnostics surfaces + the ledger); **#75** clarify.ts — an AMBIGUOUS entity row (resolver stamped `ambiguity`, not yet `confirmed`, not session-suppressed) grows the dot-scale `≟` in the `.go` glyph strip (`clarifyGlyph`) that expands INLINE to ONE dismissible human ask line (`clarifyAsk` — linked candidate vs rival + a dismiss ✗; no model/endpoint/template ids), threaded via the optional `BlockRenderArgs.clarify` the Hud owns as session state; the confirm/rival choices POST /teach/entity (write override+signal), open/dismiss are client-local (ask-once per entity per session); pinned-doc renders hydrated pins from `source:'pins'` — one row per Pin: title + kind + ingest-state why-line, `top`-capped, configured `params.doc` as the empty-state fallback; todos renders hydrated to-do items from `source:'todos'` — one row per TodoItem: text + ✓/○ status + provenance why-line (from-the-meeting vs added-by-you) + a `mark-done` verb that goes LIVE only when the item's `provenance.sessionId` is present (the PUT /todos address; hand-added items with no session trail stay inert) #15, `top`-capped, explainable "no follow-ups yet" empty state; drafts renders hydrated prepared drafts from `source:'drafts'` — one row per Draft: body + provenance why-line (act kind + source counts; endpoint dropped — human tier, #118), `top`-capped, explainable "no drafts prepared yet" empty state; teach renders SUGGESTED hint candidates from `source:'teach'` — one row per HintCandidate: the suggested match rule (humanized field + substring) + a support why-line (N reroutes → would teach ws); the `accept` verb is LIVE (carries the workspace + exact pattern → PUT /hints append-pattern, the APPLY half of the teach loop #15) while `dismiss` stays inert (candidates re-derive from reroutes each read — no dismissed-candidate store to persist to); `top`-capped, explainable "nothing to review yet" empty state; distillates renders the transcript/distillate stream from `source:'distillates'` — one timestamped row per Distillate: clock label (windowEnd) + distilled text + a human `distilled from capture` why (**PR #150** — endpoint id no longer renders at HUD tier), newest-first, `top`-capped, explainable "no distilled windows yet" empty state; fields (**#61**) renders the fan-out substrate from `source:'fields'` — one row per FieldValue: label + value + a HUMAN why-line `updated <clock>` (**PR #150**: updatedAt → provenance-window → `updated this session` fallbacks; endpoint/model/template ids NEVER render here — the full trail stays on the diagnostics surfaces + the ledger) + the #66 micro-state dot for `provisional`, `top`-capped, explainable "no fields yet" + all-dismissed disclosure; shipped on the default HUD surface as an `on-match` block AND as the #100 fields app's always-visible canon block (glyph strip live-dismiss per row via `rowAffordances` — payload {workspace, source:'fields', fieldId}; empty state points at Settings → Features in HUMAN copy — raw flag key removed, **PR #150**); queue renders honest queue status from `source:'queue'` — the injected QueueStatus row: per-kind backlog + honest ETA (basis-aware, never fabricated) + overflow policy, and the lastFailure as PROMINENT visible text (class/endpoint/verbatim server message/hint, `.rel.fail` row — never hidden), explainable "status unavailable" fallback; **#174 slice D** sense-lanes renders the composable live-sense organ from `source:'live-senses'` — the three physical lanes (Microphone / System audio / Screen) in fixed canonical order, each a health dot + `source · disposition · health` glance and a capture-freshness / processing-lag / screen-attempt why-line; a missing lane row is an honest "Status unavailable", never another lane's data; **#193** a `top` below the trio (block cap or `query.top`) paints ONLY the requested canonical prefix — a lane the document configured out gets no ever-waiting "Status unavailable" placeholder, while lanes the block does ask for keep the honest waiting row until they hydrate; registered as `BlockTypeName` `sense-lanes` + the `live-senses` `BlockQuery`/`QueryResult` source (config/surface.ts, api/payloads.ts) reusing the closed `SenseLaneSnapshot` row verbatim; reads only that closed row through `sanitizeSenseLaneSnapshot` (surfaces/sense-lane-snapshot.ts — the strict closed boundary shared by initial hydration + payload events) so no correlation id, captured content, endpoint/model, delta score, or arbitrary error reaches the DOM and a widened row degrades to "Status unavailable"; **#192** a `blocked` lane leads its why-line with the TRUE blocker in human words (`disabled` → "Turned off in Settings…", `permission-denied` → "…grant access in System Settings", `configuration-blocked` → "No model is set up for this yet…" — hud-voice, never a flag key or closed code) and the sanitizer/observation copy accept the `permission-denied` attempt outcome ("Capture refused <clock>"); **#174 slice E** `sense-lanes-replay.test.ts` renders the SAME fixture-derived canonical rows (`tools/fixtures/lane-rows.mjs` `senseLaneRowsFromFixture`) through this real renderer + `sanitizeSenseLaneSnapshot`, asserting each physical lane paints in canonical order with its OWN distinct processing lag (no merge/swap) and never leaks a capture id or captured content — the surface half of the deterministic tri-lane replay proof (the client never imports engine code, so the shared fixture projection is the bridge the engine `live-replay.test.ts` proves the real tracker emits); **PR #150 register lint** — hud-register-lint.test.ts drives every registered block renderer through the REAL renderer with marker-id fixtures and FAILS if endpoint/model/template markers render outside the Diagnostics-tier allow-list [transcript-inspector/queue/sense-gates positively asserted to SHOW ids; drafts now INCLUDED — its `via <endpoint>` why-line fixed (basics wave C, S7), guarded by a marker-carrying Draft fixture; no block remains skipped]) · hud/ (live controller + transport + dev-entry + boot.ts — **#133** the Hud controller's surface renderer is INJECTABLE (`HudOptions.renderSurface`, default `renderSurface`) so a bespoke-layout surface supplies its own without branching the controller; `notetaker-layout.ts` (`renderNotetaker`) is that renderer for the three-zone note-taker — it partitions the flat stack by the block-id zone prefix and renders each column through the SAME `renderSurface`, then composes them into the `.nt-app` grid with the rail/canvas/enrichments chrome + the relocated Record button (basics wave C S3: DISABLED with an inline "controlled from the tray · #136" note + the rail nav/home buttons disabled-with-disclosure — the interaction lint hud-interaction-lint.test.ts fails any enabled live-looking button with no wired verb / ghost / disabled marker — its wired-verb set is now read STRAIGHT from the production source of truth (`mount.ts` `WIRED_VERBS` unioned with `input-submit.ts` `INPUT_SUBMIT_VERB`), never a hand-maintained copy, so it can neither false-positive a newly wired verb nor silently bless a button whose verb was un-wired); dev-entry selects it by surface id; wired-up-arc P4: a pure boot controller retries `start()` with capped backoff until the engine answers (the packaged shell spawns its bundled engine AFTER the window loads — the old one-shot `void hud.start()` lost that race and left the transparent window permanently blank), paints every failure into a `.hud-boot-status` chip (a transparent window must never fail invisibly), reconnects the events WS with backoff and re-hydrates on `ws.open`; **#58/#96** live-transcript.ts — the EVENT-fed live strip (`transcript.updated` payloads, not query-fed): `pruneTranscript` self-expires the ~45s window on every repaint, `renderLiveTranscript` renders one row per fragment with its physical SOURCE-STREAM label `streamLabel` (`Microphone`/`System audio` — **#174 slice D** dropped the old `me`/`them` idiom because one physical input can carry several people and is never a speaker identity; **#96** each fragment attributed, never an undifferentiated interleave) + a strip-level `mute-system-stream` toggle that HIDES the system-audio lane from THIS strip (a DISPLAY filter — `Hud.toggleSystemStream` flips a client-local, session-EPHEMERAL bit; capture, the inspector, and distill are untouched; muted state discloses the hidden count + explains a system-only window); the STREAMS-STAY-SEPARATE merge rule is STATED in the file header (weaving two streams into one thread is downstream distill-accumulator territory, gated on conversation-span detection, NOT built here); **#174 slice D** `sense-lane-cache.ts` is the hud/ payload-cache seam — `patchLiveSenseResults` replaces only the matching physical source of an already-hydrated, same-scope, canonical-ORDER `live-senses` block — **#193** hydration may be any canonical-order sub-trio (a `top` below 3 keeps its live fast path per hydrated source) but a source the query never hydrated is NEVER patched in, and older/stale/cross-scope/out-of-order/duplicate/empty hydrations stay rejected — and `reconcileLiveSenseHydration` merges an in-flight re-query against payloads that landed meanwhile (rows match by physical source, newer per-source wins, the fresh query owns WHICH sources exist; a genuinely new query scope still replaces); the Hud routes `sense.lane.updated` payload-fed with no query, and a session boundary resets the live-sense cache before its catch-up query; dev-entry's `clipboardCopy` tries the async Clipboard API then a temp-textarea `execCommand` fallback, resolving only on a confirmed write so `wireActions` can report success/failure — #43; `?outline=1` (OPENINFO_HUD_OUTLINE / client.json `hudOutline`) draws the window + panel debug bounds; auto-resize.ts — wired-up-arc P4: a pure `ResizeObserver` on the painted panel (NOT the 100vh stage) reports `ceil(height)+padding` over the preload `resize` bridge, rAF-coalesced + deduped, so main content-sizes the frameless window and the old 708×720 transparent dead zone is gone) │ **#134** input.ts (the `input` block renderer — pure text-entry/file-drop shell, stamps target/submit/mode as data-attrs + empty log/context/status regions) + input-submit.ts (the imperative wiring: reads the typed text/dropped file, POSTs a turn [chat] or ingests a file [reuses pins/ingest; **basics-B/S2** the local path is RESOLVED via `webUtils.getPathForFile` on the preload bridge (`window.openinfoFiles`, exposed by preload.cts) — `File.path` was removed in Electron 32 so reading it went silently inert; `resolveUploadFile` prefers the bridge, treats a '' result as no-path (honest "needs the desktop app" failure), and falls back to a path on the File-like for the dev-harness/served-test; driven proof: scripts/attach-e2e.mjs picks a real file over CDP on real Electron], paints answer/citations/turn-budget AND a failed submit as VISIBLE text; state lives in the controller so a re-render never eats the conversation — repaint after every render) + panel.ts (attached-panel geometry: `panelSize`/`matchesTrigger` pure + `PanelController` state machine — user expand/collapse AND reveal:event opens a DISMISSIBLE suggestion on a matching bus event; reports the extent over the preload `panel` bridge → shell `hud:panel-size` → setContentSize; scripts/panel-bounds-e2e.mjs drives it on real Electron) │ (settings is ENGINE-served at GET /settings — the tray opens it in the browser, not a client settings pane) │ P6: palette/ · editor/ · **Ask face (input-submit.ts / hud.ts / dev-entry.ts):** every submit captures one frame (`captureScreen` dep → the preload bridge) and ships {screenshot, turnId}; a refused capture is disclosed beside the engine's budget note ('Screen skipped: …'); EMPTY send + frame ⇒ the message becomes the tpl-ask-default document body (`defaultAsk` dep, resolved fresh), empty send with no frame ⇒ honest visible no-op text; `ingestDelta` paints `chat.delta` frames for OUR minted turnId as a provisional streaming turn (dupe/foreign/malformed dropped) which the resolved ChatReply replaces — event-fed render, query-fed truth; `seedHistory` renders the rehydrated GET /chat/history thread once (truncated tail disclosed, a live session never clobbered); Hud routes `chat.delta` payload-fed (handled-and-returned before the refresh set, ONE event socket) to `onChatDelta`
│
├─ apps/workbench/src/          P4   Vite app served BY THE AUTHENTICATED ENGINE (browser-session cookie; cross-machine additionally requires the trusted HTTPS tunnel, with product client provisioning still pending)
│                                    ledger · archive · brief │ P6: explore (canvas) │ P7: analytics
│
├─ templates/                   P2+  the gallery — DOCUMENTS ONLY, no code (the openness proof)
├─ skills/                      P2+  shipped customization recipes a local LLM can follow (Tier A)
├─ design/renderings/           —    versioned HTML mockups (design source of truth for surfaces)
├─ docs/                        —    DESIGN-CRITIQUE (100-person org amendments; capability tiers T0–T3)
├─ spikes/                      any  throwaway proofs — never importable (see spikes/README.md)
└─ tools/                       P0   schema-gen (`gen.mjs`; `SCHEMA_OUT_DIR` overrides the output dir — unset writes the committed `shared/contracts/schemas/`, set redirects it for the #87 local drift test) · bench harness · **fixtures (#32 built):** versioned/schema-validated normalized `capture|stt|ocr|vlm` envelope; canonical ordering/serialization + SHA-256 integrity; deterministic replay clock/id factories; capture-scoped pure STT/OCR/VLM invokers (no model/network); explicit mic/system-audio/screen lanes; raw-media opt-in + sensitive owner-only paths; committed synthetic three-lane fixture consumed by the real `ScreenOcrProcessor` idempotency test; **#174 slice E** `lane-rows.mjs` (`senseLaneRowsFromFixture` + `lane-rows.d.mts`) projects a loaded fixture into the three canonical `SenseLaneSnapshot` rows — the shared, fixture-derived bridge both the engine `live-replay.test.ts` and the client `sense-lanes-replay.test.ts` assert against, so tri-lane distinguishability is proven engine→tracker→projection AND lane→surface without the client importing engine code · **#175 live gate:** `vision-live/README.md` + `client/scripts/vision-live-e2e.mjs` define the owner-run real-screen harness: generated PII-free fullscreen card → production delta gate/capture link → isolated authenticated temporary engine/store → explicitly trusted LAN OCR then workflow VLM; it checks exact owned records, operational/surface/Ask agreement, independent 3–6s scheduling, concurrent Gemma-12B-class workload, queue/latency/memory pressure, fail-closed host/model prerequisites, and writes only a private raw-frame-free report before recursively deleting transient raw queue data. **Owner-present rig PASS 2026-07-13:** four changed frames → four exact result/mirror pairs, two static delta skips, zero blank/fail/queue failures, 5001–5006 ms actual cadence, non-empty overlapping Gemma-12B completions, and raw-free Ask; the selected OCR model recognized both validation codes, so no endpoint-support follow-up is needed · evals (per-tier quality scoring, Phase-0-adjacent) · stt-accuracy (#97/#95: `say`-rendered utterances → whole-file vs fixed/overlap/vad chunking at 1/2/5s cadences against a local STT endpoint → WER table; the whole-vs-chunked delta is the transcription-accuracy regression metric; endpoints are args/env, localhost defaults)
```

## 2. Dependency rules (the ones that prevent the refactor spiral)

1. `apps/*` depend on `shared/contracts` and **nothing else in the repo**. Client never imports engine;
   workbench never imports either — they meet only at the API.
2. Only `engine/store` opens a database handle. `route/` asks store to move a session; `index/` asks store to
   write an entity. Workspace isolation is enforced by one module, so it cannot be reached around.
3. `templates/` contains no code. If a template "needs" code, a block type is missing — add it to the engine
   and keep the template a document.
4. `spikes/` is not a workspace package and is banned from imports (CI-enforced). Spikes graduate by being
   **rewritten** under `apps/` against contracts — never copy-pasted.
5. New feature → find its row in §3. If it has no row, it gets a design note in ARCHITECTURE.md *before* code.

## 3. Where future features land (so nothing invents a home later)

| Future feature | Phase | Home |
|---|---|---|
| Camera input (presence → doc capture) | P7 | `client/capture/camera.ts` + flag `capture.camera` |
| System-audio native tap (no user routing) | **#142 BUILT (loopback) / native future** | Route (b) — "no user routing" system audio — is achieved with **ZERO native code of our own** via Chromium's macOS **CoreAudio-Tap** (`getDisplayMedia({audio:'loopback'})`): `capture-renderer.ts` opens the loopback stream (drops the video track, keeps the system-mix audio) when `ShellConfig.systemAudioMethod === 'loopback'` (the macOS `auto` default, config.ts); `shell.ts` grants it via `session.setDisplayMediaRequestHandler` on the hidden capture window + enables `MacCatapLoopbackAudioForScreenShare`; `package.mjs` ships `NSAudioCaptureUsageDescription`. Rides the **Screen & System Audio Recording** TCC — a missing grant/plist yields a dead (digital-silence) stream the silence probe already flags, so a broken tap is never faked as recording (`capture-status.ts` names the grant + one-click Settings fix). Feeds the SAME source-agnostic controller/protocol/chunk/VAD path — swaps only *how the 2nd stream opens*; `device` (BlackHole detect-and-guide) stays the fallback (`systemAudioMethod=device` / pre-13 macOS). **Live capture needs a packaged, TCC-granted run to verify** (an unsigned dev electron has no bundle identity to grant against — the getDisplayMedia call stalls at the OS wall, per the #142 spike + PHASE2 aec-loopback). A from-source native `client/capture/audio-tap/` module (route (b)-native) is now only wanted if the Chromium tap proves insufficient; the inherited SystemAudioDump blob (d) stays rejected (fresh code only) |
| Diarization / voice→person | P7 | `engine/route/identity.ts` + `fabric` stt option |
| Canon + teach loop + pin ingestion | P4D (built) | **Canon** (`engine/index/canon.ts`): `mergeCanon` folds same-entity duplicates into one canonical record (union-find over intersecting name∪alias key sets; canonical winner by outboundCount→mentions→firstSeen→id; unions evidence, sums mentions/outboundCount, sets `canonicalOf`), applied at read time in `relevant.ts`. `rank.ts` gains the "sent outranks viewed" canon weight (`× (1+outboundBoost·log2(1+outboundCount))`, log-damped; unchanged at outboundCount 0 — the write side that increments `outboundCount` is deferred, drafts are prepared-never-sent). **Teach loop** (`engine/teach/`): `wireTeach` (main.ts, mirrors `wireScreenOcr`) captures every `session.rerouted` correction into a store-backed per-workspace `TeachSignal` list; `deriveHintCandidates` (pure) turns them into SUGGESTED hint patterns for the corrected-to workspace — never auto-applied to `route/hints`, never edits `route/`. **Ingest** (`engine/index/ingest/`): `chunk.ts` page-anchored chunking (the "cite p. 42" mechanism, pure), `fetcher.ts` file/url fetchers + `pdf.ts` HONEST STUB (minimal-dep policy; a vetted parser is the one future change) + gdoc seam, `ingest.ts` lifecycle → store `pins`/`pin_chunks`. Contracts: `TeachSignal`, `PinChunk`. **P4-T2 (BUILT):** all four read/write seams are now HTTP routes (`api/http.ts`, ZERO logic change) — GET/POST `/pins`, POST `/pins/:id/ingest`, GET `/pins/:id/chunks` (the "cite p.42" excerpts), GET `/teach/candidates` (`deriveHintCandidates`, read-only). Contract `HintCandidate`. **P4-T3b (BUILT):** the apply-with-review half — GET `/hints`, GET/PUT `/hints/:workspaceId` (`api/http.ts` over the `HintsDocuments` seam) — the user PUTs a reviewed candidate's pattern into a workspace's `WorkspaceHints`; the detector reads `hintsDocs.all()` fresh, so it attributes on it with no restart (no auto-apply, no `route/` logic change). Deferred: outboundCount write-side, dismiss-kind signals, PDF parser dep, gdoc auth, PUT/DELETE `/pins/:id`, DELETE `/hints/:workspaceId`, a rendered `/teach` review block. |
| Google Docs pin ingestion (auth) | P3/P4D | seam BUILT in the row above (`engine/index/ingest/fetcher.ts` `gdocFetcher`, behind flag `ingest.gdoc`); the OAuth flow + real fetch are deferred |
| Gmail/calendar write scopes | P7+ | new `engine/connect/` module (design note first, per rule 5) |
| Cloud endpoints (Gemini Live, Anthropic) | P7 | `engine/fabric/endpoints/cloud.ts` (keychain) |
| Fabric profiles + secrets (this slice) | P2 | `engine/fabric/{profiles,secrets}.ts` + profile/secret routes; live fabric = active profile |
| Settings sidebar (models/pipeline/surfaces/diagnostics) | P3 (built) | `engine/surfaces/settings/` — authenticated GET /settings, ENGINE-served SIDEBAR shell over a section registry (Status · Get started · Endpoints · Profiles · Keys · Local runtimes · Features · HUD layout · Audit ledger · Trace (#116) · Benchmarks · Try it · Privacy). The native client exchanges a one-use browser ticket for an in-memory HttpOnly session before opening the clean URL; unauthenticated access gets only a locked shell. A sidebar redesign of the old one-page /setup, which 301s here after auth. Re-homes the setup/ pure fns behind the registry. No new engine capability. #7: `sense-gates.ts` — pure `evaluateSenseGates` composes the engine-side per-sense gate chain (audio: distill.enabled → distill.transcribe → stt slot → stt-health; screen: screen.ocr → ocr slot → ocr-health), first closed gate = named blocker + fix, reusing flags + fabric slots + queue lastFailure + (route-only) live checkEndpoint. Surfaced on Settings → Status "Capture pipeline" card AND `GET /senses` (append-only route, `SenseGateChain[]`). |
| First-run / fabric setup (forms over profile+secret docs) | P2 (built) | `engine/surfaces/setup/view.ts` — pure fabric/profile/keys fragments, now re-homed into the Settings Endpoints/Profiles/Keys sections (served by the engine inside the authenticated browser session; the tray obtains a one-use ticket before opening /settings). No new engine capability. Wired-up-arc P4: **Save profile** fixed — the `#base-fabric` blob now uses `jsonForScript` (verbatim JSON), not `escapeHtml`, which had made the browser's `JSON.parse(textContent)` throw inside a raw-text `<script>` BEFORE the save fetch (a silent no-op — the core settings action was dead). `saveEditor` (`assets.ts`) is wrapped so any failure surfaces as "save failed — <reason>" in a `#save-error` strip beside the button, never silently. The per-row ✕ removes any row (existing/fresh/blank) and now survives Save. E2E-covered by `setup/save-handler.test.ts`, which drives the REAL served `SETUP_SCRIPT` against a tiny DOM shim (raw-text-`<script>` vs entity-decoded-attribute fidelity) — the gap route tests left. #123 hardening: every served POST/PUT/DELETE helper sends application/json even when bodyless (profile activate/delete, secret delete, session end), with bodyless POST+DELETE pinned in the same browser-path test. |
| Feature composition UI (flags) | P3 (built) | Settings → Features (`engine/surfaces/settings/sections/features.ts`) — every seeded flag as a human-named, stage-grouped toggle with dependency notes, flipping the EXISTING `PUT /flags/:key`. The six real gating flags are seeded from `shared/contracts/examples/flag.examples.json` via `ensureDefaultFlags` (the vision-b "enable/disable for all combinations" affordance). No new engine capability. |
| Onboarding discovery + Get-Started lens | P2 (built) | `engine/fabric/discover.ts` + probe-list/capability-map seed docs; `GET /fabric/discover` (DiscoverResult); the capability lens + one-button "Use this setup" is now Settings → Get started (`engine/surfaces/setup/view.ts`, composes the existing profile routes — no new write semantics). P4-T8: a probe `401/403` is a RESULT not a miss (`reachable+authRequired`); a probe carrying a `keyRef` (omlx `api_d`) is RETRIED with the stored secret and enumerated (`DiscoveryProbe.keyRef` + `DiscoverServer.authRequired`, additive; resolver wired through the route + Get-Started render). Seeded probe list adds omlx :8000, drops the never-present speaches. |
| Say-something verification loop (slice b) | P2 (built) | Settings → Try it (`engine/surfaces/setup/view.ts`) — "type/speak → watch it become a moment", live off the `moment.created` WS event. Engine-served page inside the authenticated browser session (the browser owns the `getUserMedia` prompt). The engine's HTTPS tunnel boundary exists, but remote product-client credential provisioning is still pending. Composes existing routes only (flags/sessions/capture/WS) — no new engine capability |
| Engine-managed local runtimes / tier zero (slice c) | P2 (built) | `engine/fabric/endpoints/local.ts` (`LocalRuntimeManager`: discover binary + spawn/ready/health/kill/bounded-restart for llama.cpp/whisper.cpp) + `local-models.ts` (download/resume/size-check) + `local-{documents,defaults}.ts` (starter catalog). invoke/health ride `local` on the existing seams (llm reuses the http chat path; stt speaks whisper.cpp's `/inference`). Routes `GET /fabric/local/models` + `POST /fabric/local/download`; the nothing-found lens offers "Download a starter model" → writes a `local` endpoint into config-1 via the existing profile routes. P4-T8: `RUNTIME_SPECS.mlx` (omlx) is BUILT — an `adoptOnly`, `multiModel` runtime on a FIXED port (:8000): the engine DISCOVERS-AND-ADOPTS the externally-managed server (oMLX.app + the `com.openinfo.omlx` LaunchAgent) rather than spawn-and-collide, and leaves it running on shutdown. The `local` Endpoint variant gained optional `keyRef` auth (omlx needs a bearer even on localhost; `resolveLocal` carries it, `checkEndpoint` live-probes the fixed port with it). Seeded templates naming fictional models (`local-model`, `llama3.2:3b`) are GONE — discovery/scan are the truthful source, one blank manual scaffold remains. ollama/paddle/coreml are future specs (CONTRIBUTING recipe). #126 follow-up: `LocalModelStore` now shares the manager's injected runtime-discovery seam (`createEngineApp` threads `localRuntime.findBinary`/`.specs` into BOTH), so `runtimeAvailable` honors the injected resolver instead of its own PATH lookup — ONE injection governs spawn AND availability, and the CI `llama-server` PATH stub is gone (the e2e no longer depends on a real binary) |
| Invoke resilience + honest errors | P3 (built) | `engine/fabric/invoke-error.ts` (`InvokeError` taxonomy: unreachable · timeout · auth · model-load · bad-response · reasoning-exhausted — each with a troubleshoot hint; `AggregateInvokeError` keeps the classes through fall-through) + `diagnose.ts` (classify a drain throw → `QueueFailure`, enrich a model-load hint with the loaded-model suggestion via `discover.ts` `listLoadedModels`, read-only). `GET /queue` (`QueueStatus` + `lastFailure`/`lastSuccessAt`, drain state in-memory on the CaptureQueue). `POST /fabric/test` `probe:'generate'` runs a REAL completion (the ping-lied fix; `EndpointProbe.generate`); a reasoning model that spends that budget thinking is generation ✓ with a note — the model loaded and generated, which is what the probe proves. Wired-up-arc P1: the prompt is now a real, model-answerable question at maxTokens 128 (not `'ping'`@1) and `GenerateProbe.sample` carries the model's ACTUAL reply (truncated) so Test shows proof + latency, not a checkmark. Try-it three-truths = pure `setup/view.ts` `tryItDiagnosis` reading `GET /queue`. Status shows the classified last failure. No auto-switch (user agency). |
| Token accounting + Audit ledger | M5 #65 (built) | Every invoke records tokens in/out + duration into provenance, and Settings → Diagnostics → **Audit ledger** renders the per-pass hop trail. Contract: `InvokeUsage` (`shared/contracts/src/common.ts`, `$id`) appended optional onto `Distillate.provenance` + `OcrResult.provenance` (APPEND-ONLY — old records still validate). Capture: `fabric/invoke.ts` `buildUsage` — measured from the API `usage` block (`estimated:false`, total derived from halves when omitted) else chars/4 estimate + MARKED (`estimated:true`); wall-clock `durationMs`; generic so the #62 judge (via invokeLlm) picks it up automatically. Threaded by `distill/distiller.ts` + `screen/processor.ts`. View: `surfaces/settings/sections/ledger.ts` (`buildLedger` pure + `renderLedger`) — table columns when·stage·endpoint/model·tokens·guard·egress; GUARD (#63, now BUILT) renders each hop's verdict — clean · redacted·N (span kinds on hover) · unguarded — and "— no guard" only when a hop genuinely carries no verdict. EGRESS (#64, now BUILT) renders from each pass's recorded decision: "local", "local · &lt;layer&gt;" when it stayed local by policy, or a distinct "egress" when content actually left; a pre-#64 record with no decision falls back to the honest local default. DRIVEN served test `api/settings-ledger.test.ts` (boot engine → seed → GET `/settings/ledger` → assert rendered text) per the QA rule. **#116 multi-hop:** `buildLedger` gains optional extras — a window's moments fold onto its distill pass as ONE aggregated `moments` hop (spanId join, else the distillateId parent), each FieldValue is a pass whose trail gains a `judge` hop once reviewed, and a GuardHold is a `held` pass naming the guard endpoint and honestly NO model endpoint ("held before send") — flat behavior without extras is unchanged. STT invoke timing now has a provenance-record home (SttSegment.durationMs, #116); token counts for audio invokes remain unreported by servers. Deferred: workspace picker (default workspace only). |
| Audit trace: one input → every engine hop → output | #116 (built) | An auditor picks ONE input (utterance segment or screen capture) and walks it transcribe → distill → moments → fields → judge → guard/egress. Correlation: every pipeline pass mints a `spanId` shared by all records it produces (distiller per merge window incl. GuardHold; fields per fan-out batch; judge per review batch; screen per OcrResult+mirror pair; transcribe per drain), plus deterministic parent links (`SttSegment.chunkId` ↔ `Distillate.sourceChunks` ↔ `FieldValueProvenance.sourceChunks`, `GuardHold.sourceChunks`) — NEVER fuzzy time matching. STT root: `records/sttSegment.ts` persisted per transcribed chunk at the `onTranscribed` seam (`api/http.ts` runTranscribe; store `stt_segments` table) — chunk id, lane, capturedAt→processedAt, textChars (size, never the text), endpoint/model/durationMs/egress. View: Settings → Diagnostics → **Trace** (`surfaces/settings/sections/trace.ts`, pure `buildTraceInputs`/`buildTrace`/`renderTrace`; registry `trace`) — server-rendered picker links (`?input=<id>`) → the hop chain with guard verdicts + egress decisions (reuses the ledger's `guardCell`/`egressCell`); every state is TEXT (empty · unknown id · no-steps-yet · a failed store read surfaces its true reason via the route's try/catch `problem` seam). Ledger multi-hop: see #65 row. DRIVEN served e2e `api/trace-e2e.test.ts` (real server + fake stt/llm/judge on loopback → real /capture/mic POSTs → the page's own links → full chain + both sabotage paths render text); fixture-level `sections/trace.test.ts`. Live-rig walk (real parakeet utterance) is owner-gated — deterministic replay stands in. Disclosed: pre-#116 records lack the links and are named non-walkable on the page; default workspace only (the ledger's scope). |
| ContextPackets: converge separate senses, inspectable | #176 slice 1 (built; diagnostics rendering = slice 2) | A typed, versioned `ContextPacket` (`records/contextPacket.ts`) converges one correlation window's mic + system-audio + screen observations as REFS (never copies — no prose field; prose stays a read-time view over the refs), with focus/app evidence from the Session record, entity candidates (in-window moments + #74 screen corroboration with ocrId/form/similarity), machine-readable `gaps` for missing senses, deterministic lane-count confidence, and an append-only `revision`/`supersedes` chain (a late observation appends, never mutates). Built by the PURE deterministic correlator `index/packets.ts` (`buildContextPackets`) over stored records; persisted per-workspace (`store/workspaces.ts` `context_packets`, carried whole-chain by moveSession); queryable by workspace/session/time-window/entity over GET `/context/packets` + built on demand via POST `/context/packets/build` (returns only appended packets; [] = idempotent no-op). Determinism proven over the #32 fixture: same fixture replayed twice ⇒ byte-identical packets (`index/packets-replay.test.ts`). **Slice 2 (built):** a LIVE producer (`index/produce-packets.ts` `materializeContextPackets` + `PacketBuildLog`) wired at the `session.ended` seam so packets materialize during normal capture with no route call — CONTAINED (a build failure is caught, recorded, never fatal to capture) + idempotent + batched per session + workspace-isolated; and the diagnostics surface (`surfaces/settings/sections/packets.ts`, Settings → Diagnostics → **Context packets**) rendering membership/exclusions/timing/confidence with the supersession chain and the honest "last update" / "last update didn't finish" line — every value traceable, every state TEXT (empty · assembly `problem` · contained build failure). Proven served in `api/context-packets-e2e.test.ts` (session end → the SERVED page shows the converged window, no build route) + `sections/packets.test.ts` + `index/produce-packets.test.ts`. |
| Layered egress-consent policy | M5 #64 (built) | Egress (may content leave the machine?) resolved across FOUR layers, any of which can DENY, most-specific denial wins, every decision records which layer won — the voice-binding precedent. Contract: `shared/contracts/src/config/egress.ts` — `EgressReach`(local/egress) · `EgressLayer`(endpoint/prompt/mode/workspace/content-class/default) · `ContentClass`(screen/transcript/typed/unknown) · `EgressPolicy`({deny}) · `EgressDecision`({reach,allowed,decidedBy,reason}); appended optional onto `Mode`/`Workspace` (`egress`), `PromptTemplate` (`neverEgress`), `Distillate`/`OcrResult` provenance (`egress`), and `QueueFailure.class` gains `egress-denied` (all APPEND-ONLY). Resolution + layer-1 classification: `fabric/egress.ts` (pure). Enforcement: `fabric/invoke.ts` `egressGate` at the endpoint-choice seam — filters egress endpoints when consent denies, `egress-denied` classified failure surfaces the reason, guard #63 hooks where the gate allows an egress reach. Wired: `distill/distiller.ts` (transcript class, mode/workspace/prompt denial, stamps provenance) + `screen/processor.ts` (screen class always denies). Factory posture: no egress endpoint configured ⇒ nothing can leave. Deferred: no UI to set mode/workspace `egress.deny` or prompt `neverEgress` yet (contract + engine honor them; a Settings toggle is a later slice); typed-content class not distinguished from transcript in v0 (both may egress). **#91 QA** `distill/privacy-gate-seam.test.ts` drives the CONSENT gate through the real distiller: a denying MODE / never-egress PROMPT holds the whole window (egress-first endpoint gets ZERO hits across summary+moments+entities, local fallback answers all three — the dropped-wrapper sentinel), no-fallback fails closed, `decidedBy` recorded on provenance. **#128 (FIXED):** the WORKSPACE layer (layer 3) now fires at the distiller seam — `store/workspaces.ts` persists + rehydrates `Workspace.egress` through the meta-DB row (additive `egress` JSON column + idempotent `pragma`-guarded ALTER migration; `fromRow` no longer drops it), and `setEgressPolicy(id,policy)` is the write path (no UI yet — a Settings toggle is a later slice). The seam suite gained the fourth-layer case: a workspace deny holds the egress hop end-to-end and stamps `decidedBy:'workspace'` on provenance. |
| Egress guard: content/PII filter on egress hops | M5 #63 (built) | A first-class `guard` fabric SLOT KIND (`shared/contracts/src/config/fabric.ts` — optional array on `Fabric.slots`, append-only; NOT an `llm.judge`-style name convention #88) invoked on every ALLOWED egress hop. Contract (`shared/contracts/src/config/guard.ts`): `GuardBehavior`(redact-and-continue\|hold-and-surface) · `GuardSpan`({kind,start,length} — a descriptor, NEVER the raw value) · `GuardVerdict`({outcome clean\|redacted\|held\|unguarded, behavior, guarded, maskedSpanCount, spans?, guardEndpoint?, reason}) · `GuardPolicy`(the verdict→behavior document, {behavior, acknowledgeUnguardedEgress}) · `GuardHold`(a suspended hop's durable audit record); appended optional onto `Distillate.provenance` (`guard`), `QueueFailure.class` gains `guard-held`, event `guard.hold.updated`. Core (`engine/fabric/guard.ts`, pure + a small openai-compat classifier client): `applyRedaction`/`redactMessages` (mask spans back-to-front with `[redacted:<kind>]`, offset-mapped across the joined messages) · `evaluateGuard` (the whole decision table incl. the fail-closed empty-slot edges) · `classifyEgressText` (POSTs the outbound text to a guard endpoint, parses `{flagged:[…]}`) · `runEgressGuard` (classify→evaluate→redact-or-throw; a configured-but-unreachable guard FAILS CLOSED) · `GuardHeldError` (class `guard-held`). Interception: `fabric/invoke.ts` `invokeLlm` runs it at the documented `{allow:true,reach:'egress'}` hook — redact-and-continue masks the outbound copy + proceeds (verdict on `LlmResult.guard`); hold-and-surface / fail-closed HOLDS by throwing (a HARD STOP — never a silent local reroute). A LOCAL hop never enters it (no egress ⇒ no filter). Policy/holds store: `engine/guard/documents.ts` (`GuardDocuments` seeds the default redact-and-continue/not-acknowledged policy; `GuardHoldStore` — one `_meta.db` doc/workspace, the ItemSignalStore pattern; `resolve` flips release/denied). Wired: `distill/distiller.ts` builds the guard opts from the policy + the `guard.egress` flag + the fabric guard slot, threads it onto every window invoke, and on a HOLD records a `GuardHold` + publishes + SKIPS the window (no distillate — nothing left). Routes (`api/http.ts`): `GET /guard-holds` · `POST /guard-holds/resolve` (release/deny) · `GET`/`PUT /guard/policy` (validated). Ledger (`surfaces/settings/sections/ledger.ts`): the GUARD column lights up from `provenance.guard` (clean · redacted·N · unguarded · "— no guard" for a local hop), and held hops surface in a held block with a release/deny affordance (inline POST). DRIVEN served tests `api/settings-guard.test.ts` + `fabric/guard-invoke.test.ts` (fake guard endpoint) + `distill/guard.test.ts`; **#91 QA** `distill/privacy-gate-seam.test.ts` drives the guard END-TO-END through the real `Distiller`+`invokeLlm` against fake egress/guard HTTP endpoints (clean rides provenance + content leaves · redact-and-continue · hold-and-surface SUSPENDS + records/surfaces a GuardHold + release/deny · fail-closed empty slot · guard runs on EVERY egress call summary+moments+entities · a LOCAL hop is never guarded) + the ledger renders that seam output. Gated by `guard.egress` (default OFF). Deferred/disclosed: real llama-guard safe/unsafe→spans is a follow-up adapter (v0 speaks one `{flagged}` JSON shape); the guard intercepts only the LLM text-egress path (screen never egresses, stt audio isn't a text-span filter); AUTO re-driving a released hold is deferred (the raw content is not retained — release marks it resolved + surfaces; the operator re-runs / switches to redact); local/cloud guard endpoints skipped in v0 (http/openai-compat only); no Settings UI to edit the policy yet (the PUT route + document exist). |
| Entity contract v2 (evidence + sovereign overrides) | M5 #73 (built) | The additive Entity surface the resolver (#72, NOT YET BUILT) needs. Contract (`shared/contracts/src/records/entity.ts`): append-only optional `confidence`, resolution `state` (read by the #66 micro-state dot), `ambiguity` (plausible-rival marker), `heardAs[]` (source-/confidence-typed ASR variants that resolved here), `sightings[]` (typed heard/seen/calendar evidence trail), `overrides[]` (first-class user corrections that outrank scores), `external` (code-host/CRM linkage); sub-schemas registered in `AllSchemas`, `pnpm gen` regenerated (5 new schema JSONs). Store (`engine/store/workspaces.ts`): `upsertEntity` appends a typed sighting + an ASR heardAs per mention (append-only, deduped/idempotent); `mergeEntity` carries all v2 fields forward (a confirmed record STAYS confirmed). New `overrideEntity(ws,id,override)` — the SOVEREIGN write path: appends the override, stamps `state:'confirmed'`+`confidence:1`, pins the surface form as an alias; `findEntity` PREFERS a pinned mapping over any rival so resolution is deterministic and never re-decided against a settled rival (the store half of the #72 short-circuit). Populate: `distill/distiller.ts` records `via:'heard'` sightings + `stt` heardAs from the transcript window (the only LIVE producer). Render: `client relevant-now.ts` threads `entity.state` through the existing #66 dot (no new renderer). DISCLOSED/awaiting #72: state/confidence left absent by plain extraction (no scorer yet); per-variant ASR confidence not surfaced by STT (left undefined, never faked); seen/calendar sightings await their entity paths; `external` has no auto-populator yet; **#75 (BUILT):** `overrideEntity` is now wired to the `POST /teach/entity` route + the ≟ clarify affordance, and CLEARS `ambiguity` on settle (companion `clearEntityAmbiguity` settles the losing side of a disambiguate); `moveSession` does not migrate the v2 evidence trail. Tests: store (+3 evidence/override/migration), client relevant-now (+1 state dot), contracts (+1 v2 example; v1 example proves migration). **#91 QA** `distill/evidence-seam.test.ts` drives the population through the real distiller+`invokeLlm`: a resolved mention writes a `heard` sighting with the WINDOW-derived timestamp (`sighting.at === windowEnd`, distillate-tied) + a `stt` heardAs; the #74 crossSighting path (heard mangle + same-window OCR) records a `seen` sighting, confirms, and teaches the alias. |
| App bundles (bundle-as-runtime-object) | P4 (built) | "Defaults are just documents we ship" — an app is a DOCUMENT composing references to its organs. Contract (`shared/contracts/src/config/bundle.ts`): `Bundle` ({id·name·version·description?, `faces`[≥1], `workflowRef`?, `templateRefs`?, `flags`? overlay, `chat`? assembly}) · `BundleFace` ({kind·surfaceRef·title?}) · `BundleFaceKind` (hud\|chat\|support — the face vocabulary the Surface DSL targets) · `ChatContextAssembly`/`ChatContextSource`/`ChatContextSourceKind` (the SEVEN declared chat context sources + honest per-source budgets, "data not code"); all append-only/closed-union, everything past faces optional, `pnpm gen` regenerated (6 new schema JSONs). Engine (`engine/bundles/`): `BundleDocuments` store + `loadDefaultBundle` + GET/PUT `/bundles`[`/:id`] routes (document-route idiom, Tier-A validation gate, seeded on boot). Seed: `bundle.standard-app.json` — the MVP Standard App (the pill) composing ONLY existing organs; `seed-integrity.test.ts` proves every ref resolves to a served surface/workflow/template/flag. Client (`client/main/app-catalog.ts` + shell `refreshBundles`): the tray Apps folder lists the bundle as ONE app whose faces open the mapped surfaces (open-app via the one window factory — titles come free); unclaimed surfaces demote to standalone catalog rows. Read-only consumption. **pill P1 (BUILT):** the chat context-assembly READ path — POST /chat resolves the governing bundle and `engine/api/context-assembly.ts::assembleChatContext` iterates the DECLARED sources in order under their honest budgets, with per-source accounting (included/capped/empty/unavailable) folded into the visible budget note; a bundle-doc edit changes assembly with NO code change; the `active-preset` read-seam is defined but left for P2 to fill (degrades to `unavailable`). DEFERRED (out of scope): pill assembly/geometry, flag-overlay injection wiring, the Surface DSL spec+compiler, any bundle-editing UI. **UPDATE (pill P2):** the PRESET-injection half is now BUILT — the five editable presets are actual documents (`engine/presets/`) selected per workspace and ACTUALLY prepended to the distill pass; see the `presets/` row + the distill preset-injection note. **UPDATE (Ask face):** the union grew to EIGHT — `screen`, the send's one frame read into text (ocr slot under screen-class consent, egress denied outright); the shipped plan declares it and an unedited seeded bundle refreshes to it (seed-or-refresh); streamed replies (`chat.delta`), the explain-my-screen document (`tpl-ask-default`), and the persisted per-workspace thread (+ GET /chat/history) complete the Ask vertical — see the api/·store/·distill/ rows. |
| Entity resolver: scored phonetic/fuzzy match | M5 #72 (built) | The match step, replacing exact normalized-string equality so an ASR-mangled mention ("pie dev" for `pi.dev`) finds its record. Pure primitives (`engine/index/phonetic.ts`, zero deps): levenshtein/editSimilarity, a compact Double-Metaphone (primary+secondary), phoneticEqual, and `nameSimilarity` (token soft-Jaccard-led — order-independent + penalizes unmatched tokens so "Sam Lee"/"Sam Rivera" stay below the link floor; whole-string edit sim only when token counts differ, the split/join case). Resolver (`engine/index/resolve.ts`, pure): `resolveEntity` returns `{match?,score,band,ambiguous,components,rival?,margin?}` from `score = phoneticFuzzy(over name/aliases/**heardAs**) × corpusPrior × crossSourceCorroboration × personAffinity` (clamped). Bands: ≥0.85 auto (silent link) · 0.5–0.85 provisional (reviewable #66 micro-state) · <0.5 new provisional entity; a plausible rival (≥0.5) within ambiguityMargin 0.05 of the winner ⇒ `ambiguous` (auto downgraded to reviewable, rival named for the clarify affordance #75). corpusPrior (sighting count × recency) ONLY boosts + is bounded, so it never drags an exact match below auto. Extraction path (`engine/store/workspaces.ts` `upsertEntity`): the SOVEREIGN override short-circuit runs FIRST (pinned surface form outranks any score — #73 invariant preserved) and honors `rejectedRivalId`; else the resolver decides. A FUZZY link writes the corrupted heard form to `heardAs` (the write-back), NOT to `aliases`; every resolution (link or create) appends an `EntityResolution` (score+band+4 components+rival) to provenance; a provisional/ambiguous link stamps `state:'provisional'`+`confidence`+`ambiguity`, a clean auto-link and a first-of-its-kind create stay silent (exact matches resolve byte-identically to pre-resolver), a `confirmed` record is never downgraded. `moveSession` stays exact `findEntity`. Contract: `EntityResolution` + optional `resolutions[]` on `Entity` (append-only; `pnpm gen` re-ran). DISCLOSED: `crossSourceCorroboration` (#74) + `personAffinity` (entity graph) are neutral-1.0 INPUT MULTIPLIERS with no producer yet (typed seam on `EntityUpsert.signals`); the distiller passes no signals yet; **#75 (BUILT):** the clarify/correction UI + write path now rides this — the resolver's `ambiguity` marker is what the ≟ affordance keys off, an answer writes a sovereign override recording `rejectedRivalId`; Double-Metaphone is a compact ASR-tuned implementation, not a reference port. Tests: phonetic (+8), resolve (+11 incl. the mangled-names fixture + exact-match regression), store (+3). Engine 631, contracts 77. |
| Clarify affordance + entity-correction teach kinds | M5 #75 (built) | The ambiguity ask + its write path, riding #72's `ambiguity` marker and #73's `overrideEntity`. **Affordance** (`client/surfaces/blocks/clarify.ts`): an ambiguity-gated dot-scale `≟` in the #66 glyph idiom — `clarifyGlyph` renders it on a row only when the entity carries a named rival, is not `confirmed`, and is not in the Hud's session `suppressed` set; `clarifyAsk` expands ONE inline dismissible line (NEVER a modal) offering the linked candidate vs the rival + a dismiss ✗, in HUMAN copy (heard form + entity NAMES, no model/endpoint/template id — #117). Threaded via the optional `BlockRenderArgs.clarify` (`{suppressed, expanded}`) the `Hud` owns as session-ephemeral state (`openClarify`/`dismissClarify`/`settleClarify`, mirrors the #96 mute bit); `mount.ts` gains `clarify`/`clarifyOpen`/`clarifyDismiss` (answer paints ✓/! on the choice; open/dismiss client-local). ASK-ONCE: answered/dismissed ⇒ the id enters `suppressed` (no ≟ this session), and a confirmed answer clears the record ambiguity so it does not re-appear across reloads. **Write path** (contracts + engine): `TeachSignalKind` += alias-confirm/alias-reject/rename/disambiguate/dismiss (append-only); `TeachSignal` reroute fields optional + optional `entity` (EntityCorrectionSignal) variant, one TeachStore (entity corrections file under the entity workspace); `POST /teach/entity` (`EntityCorrection`→`Entity`) writes a sovereign `EntityOverride` (pins the heard form, records `rejectedRivalId` so the wrong rival is never re-offered) AND a labeled `TeachSignal` — confirm pins to the linked candidate, disambiguate pins to the RIVAL and settles the once-linked row; `overrideEntity` clears `ambiguity`, `clearEntityAmbiguity` settles the losing side. Provenance engine-stamped (`at`/id/`by:'the user'`), suggest-never-auto-apply. Tests: clarify.test (7), action-verbs driven (+3), http (+2 over the real engine), store (+3), teach signals (+2), contracts (+2 examples; `pnpm gen` re-ran, 2 new schema JSONs). Suites: contracts 81 / client 363 / engine 667. DEFERRED: alias-reject ("new") needs a resolution-bypassing create + rename needs a rename surface (declared-not-emitted); dismiss union entry awaits the #66 dismiss surface; correlation-id stamping rides #116. |
| Human why-lines on HUD-tier blocks | #117 (built) | The HUD says WHY an item is relevant in human terms, never in machine ids. `client/surfaces/blocks/relevant-now.ts` `provenanceWhy` no longer renders `via <endpoint> · <model> · <window>`; a recorded-provenance row now states the source kind (heard / on screen / from calendar — from the entity's latest `sighting`, else the honest `heard` default, NEVER the fabric slot which is a capability name) + a recency clock (provenance window end, else `lastSeen`). The heuristic fallback (mention-count + latest moment) was already human and is unchanged; copy affordances unchanged. The full machine trail (endpoint/model/window/tokens) stays RECORDED on the entity and reachable on the DIAGNOSTICS-tier surfaces (`fields.ts` `via endpoint · model · template id`, `transcript-inspector.ts` stt slot) + the audit ledger (#65) — deliberately untouched, that density is their job. `moments.ts` / `now.ts` never rendered endpoint/model (now explicitly states "no engine labels"); the #75 clarify ask already complied. Regression: the served/driven `renderer.test.ts` HUD render asserts the ABSENCE of any endpoint/model id and the old `via …` phrasing, plus the new `heard · <clock>` / `on screen · <clock>` copy. |
| Public-name gazetteer (OSS-collision rival source) | #143 (built) | The clarify gate's missing RIVAL SOURCE: without a concept of a plausible OUTSIDE name, a corporate repo that reused a famous OSS name (an internal "Kubeflow" repo vs the public Kubeflow) never trips ≟ — the resolver silently links to the corpus entity. **Seeded document** (`engine/index/gazetteer-seed.ts`): `DEFAULT_GAZETTEER` — a compact static v0 list (~30 broadly-known infra/data/ML OSS + platforms with spoken aliases: Kubernetes/k8s, Kubeflow, Kafka, PostgreSQL/postgres, TensorFlow/tf, …), stored under kind `gazetteer`/key `gazetteer-default`. Seed-if-absent into `_meta.db` on first read (`WorkspaceRegistry.gazetteer()`), user-editable, NEVER clobbered (a `put` runs only when absent), install-wide. No network fetch (minimal-dep + deterministic, inspectable rival source). **Pure matcher** (`engine/index/gazetteer.ts`): `gazetteerRivals(heardForms, doc, {kind,at,floor})` reuses `nameSimilarity` to build synthetic RIVAL-ONLY Entities (stable id `gaz:<slug>`, `mentions:0` ⇒ neutral corpusPrior so a public name can never out-boost an established corpus entity — it ties, and a tie is what we ask about) for entries clearing the floor (default `provisionalBand`). **Resolver hook** (`engine/index/resolve.ts`, additive): `resolveEntity` gains an optional `rivals` input — candidates scored + eligible to be NAMED as the runner-up but NEVER selected as winner/match, and consulted only in the LINK branch (a gazetteer hit against a `new`-band resolution changes nothing ⇒ gazetteer-only stays SILENT). They honor `rejectedRivalId` like any candidate. Empty `rivals` ⇒ byte-identical to pre-#143. **Store hook** (`engine/store/workspaces.ts`): `resolveMention` asks the matcher for rivals and passes them to the resolver; a corpus link within `ambiguityMargin` of a public name is stamped `ambiguous` with `ambiguity.rivalId:'gaz:<slug>'`+`rivalName`, so the existing ≟ affordance (#75) fires and a `confirm` answer pins the heard form to the repo + records `rejectedRivalId:'gaz:<slug>'` (the sovereign short-circuit then resolves the settled form clean forever). Zero new deps, zero contract change (works within EntityAmbiguity/EntityResolution's existing named-rival fields; `rejectedRivalId` is an `Id` string that carries the synthetic id fine). Tests: `index/gazetteer.test.ts` (10: stable slug · offers/alias-matches/stays-quiet · floor respects band · corpus-vs-gazetteer ⇒ ambiguous rival named · gazetteer-only silent · rejectedRivalId honored · no-rivals regression guard), `store/workspaces.test.ts` (+3: the "cube flow"/Kubeflow e2e through the store incl. the confirm-answer override + never-re-asks · gazetteer-only create stays silent · seed-if-absent survives a user edit + reopen). Suites: contracts 81 / client 363 / engine 706. DEFERRED: a GET/PUT `/gazetteer` route (the doc is editable over the generic LayoutStore today; a typed route + contract is a follow-up); a `disambiguate`-to-gazetteer answer (choosing the public project) would pin to a non-existent `gaz:` host — semantically "reject the corpus link", left for a create-bypassing path; static seed only (no auto-population). |
| Chunking architecture (word-boundary fix) | #95 (built) | The 0.0.8 default sliced audio into FIXED 1s segments transcribed independently; a clock cut lands mid-word ~1×/s and the model fabricates a phantom word ('testing testing testing' → the boundary chunk 'thing.'). Measured with `tools/stt-accuracy` (say-rendered fixtures, WER vs whole-file, parakeet + whisper both localhost): fixed-1s ~0.20 WER, fixed-5s still corrupts a boundary word in a continuous monologue (~0.04), **vad (cut at pauses) 0.00 — matches whole-file**. Overlap+merge was measured and REJECTED (naive exact-word-overlap merge duplicates the boundary span → ~0.69 WER). Shipped: `client/capture/vad.ts` (PURE `shouldRotate`/`nextSilenceRunMs`/`resolveVadParams` + `DEFAULT_VAD_PARAMS` chosen from the measurements) driven by the renderer's AnalyserNode amplitude poll — rotate the MediaRecorder at a detected pause (past a min, with a max cap bounding pauseless-speech latency), never mid-word. Strategy + knobs are user-config (`ShellConfig.chunkStrategy`/`vad*`, env>file>default, default `vad`; `OPENINFO_CHUNK_STRATEGY`/`OPENINFO_VAD_*`), threaded via `CaptureStartOptions`. No engine merge needed (segments are already clean), so the #58 transcript.updated fast path is unchanged. Trade: an utterance ships ~silenceHoldMs (400ms) after the speaker pauses; pauseless worst case = maxSegmentMs (6s). Tests: `vad.test.ts` (6), `capture-renderer.test.ts` vad rotation + max-cap (2), `config.test.ts` strategy/knob resolution. |
| STT interop seam (canonical transcript + adapters) | P4-T9 (built) | `engine/fabric/stt-adapters.ts` — the engine owns transcript normalization: a canonical `TranscriptResult` (`text` — '' is valid silence — plus optional `language`/`durationSec`/`segments` in canonical SECONDS) + per-flavor adapters (`STT_ADAPTERS`), each owning its request shape (path, whether the `model` field is sent, `response_format`) and a `normalize` that maps the wire body to canonical form. `selectSttAdapter` chooses by endpoint api/runtime: http `openai-compat` → OpenAI transcription; local `whisper.cpp` → whisper-server `/inference` (centisecond `t0`/`t1` → seconds); local `mlx` → omlx transcription. `invokeStt` picks the adapter and speaks only the canonical shape — a new STT engine is an adapter + the CONTRIBUTING recipe, never a new invoke branch. Fixes the ONE broken pipeline: a `local` mlx (omlx) STT endpoint used to hard-fail ("no transcription path") and never sent the served-model id omlx REQUIRES; it now adopts :8000, POSTs `/v1/audio/transcriptions` with the model + keyRef→bearer. Live-verified against omlx 0.4.5 on the rig (its whisper model returns the OpenAI verbose_json shape the omlx adapter normalizes); omlx 0.4.5 itself rejects PARAKEET for stt ("Model type … not supported for stt"), which the seam surfaces honestly as a classified `model-load` failure — a server-side limitation, not an engine gap. Silence filter (#69): `TranscriptSegment` carries an optional whisper-class `noSpeechProb` (openai/omlx now request `verbose_json` to receive per-segment `no_speech_prob`), and the pure `dropSilentSegments` drops near-silent/hallucinated segments (default `DEFAULT_NO_SPEECH_THRESHOLD` 0.8) before `transcribeChunks` lets any text enter the distill accumulator — parakeet-class (no signal) drops only empty/whitespace segments. |
| Host-scan + model dropdown (Endpoints editor) | P3 (built) | `engine/fabric/scan.ts` (`scanHosts`: exact-url, or bare-host × the probe-list DOCUMENT's ports; models classified via the capability map; failures in the invoke taxonomy's classes; a 401/403 = reachable+authRequired; fresh per call, value-free) + `POST /fabric/scan` (`ScanRequest`/`ScanResult`). The Endpoints editor's per-row **Scan** button turns the free-text model field into a grouped dropdown (slot-matching models first, capability chips, a custom… escape hatch), renders the per-host capabilities summary ("30 chat · 4 ocr · 2 embed"), highlights the keyRef selector on authRequired, and offers "scan common ports on <host>" for a bare host / dead URL. Pure decisions in `setup/view.ts`, mirrored by the browser script. Wired-up-arc P2: the row's URL is now SEPARATE scheme/host/port fields (`parseEndpointUrl`/`composeEndpointUrl`, IPv6-aware, path-preserving via `data-urlrest` so a url round-trips losslessly; stored `Endpoint.url` shape unchanged) — a same-host LM Studio→omlx port swap is one field; scan reads host+port (blank port ⇒ scan the common ports on the bare host). |
| Local runtimes (Settings → Models) | P2 (built) | `engine/surfaces/setup/view.ts` `localRuntimesHtml` + `settings/registry.ts` `localRuntimesBody`. Wired-up-arc P2: renders the runtime servers discovery FOUND on this machine (the same `DiscoverResult` the Get-started lens uses) — each with name/flavor, reachable/needs-key state, and its models grouped by slot guess, so parakeet-class stt on an adopted mlx/omlx server is finally visible here — ABOVE the download catalog (starter llama.cpp/whisper.cpp). Discovered servers render as adopted (managed externally over HTTP), never downloaded; the route runs discovery for this section too. |
| Screen capture + OCR/VLM understanding | P4B (built) | Client `capture/` screen source (main-process `desktopCapturer`, opt-in `OPENINFO_SCREEN`; production `FrameDeltaGate` before transport; kept image chunk + companion `ScreenFrameMeta` carrying `deltaScore`) → fabric `invokeOcr`/`invokeVlm` (`fabric/invoke.ts`: `paddle-serving` OCR + `openai-compat` VLM, VLM-transcription fallback fills the ocr slot; raw-frame non-OK bodies cancelled unread) → `engine/screen/` processor (rides capture ingest, stamps an `OcrResult` + a mirror distillate; own bounded failure ring) + router (`GET /screen/results` · `GET /screen/status`, one mount line in `api/http.ts`). Contracts: `ScreenFrameMeta`, `OcrResult`, `OcrInvokeParams`/`VlmInvokeParams`, `ScreenStatus`. Store: `ocr_results` table. Folded into the workflow executor as an `ocr`/`vlm` drain stage (the P4A×P4B joint slice, DONE 2026-07-08 — screen understanding is executor-owned when `workflow.enabled` is ON, ingest-owned when OFF; one owner, no double-processing); #175 makes status/diagnostics use that same active-owner plan, including custom/ungated workflow flags and healthy fallback endpoints. **#175 acceptance:** deterministic valid-JFIF coverage proves legacy OCR + workflow VLM end-to-end through persistence, routes, surfaces, and screenshot-free Ask. The owner-present rig run passed 2026-07-13: two legacy OCR + two workflow VLM real frames produced exact result/mirror pairs, two unchanged frames delta-skipped, status/lanes/surface/Ask agreed, and zero blank/failed/queue-failure states were observed under overlapping Gemma-12B pressure. Deferred: managed-local `runtime:'paddle'`. |
| Calendar as a routing signal | P4C (built) | The second staged routing signal after focus. Contracts: `CalendarSignal` (`api/payloads.ts`: eventTitle + optional attendees/calendarName/startsAt/endsAt), `AttributionPattern.field` += `eventTitle`/`attendee` (`config/hints.ts`), field→kind `eventTitle`/`attendee` → `'calendar'`. Engine: `route/calendar.ts` (pure decode of a raw Calendar.app sample → `TimedCalendarSignal[]`) + `route/calendar-collector.ts` (`CalendarPoller` engine-side osascript/JXA poll of the current/imminent event + `startCalendarCollector(app,…)`, mounted from `main.ts` like `wireScreenOcr`). `detector.ts` scores focus+calendar in ONE `TimedSignal` sustain window; `attribute.ts` buffer/`observe` take `TimedSignal`; `attributor` exposed on `EngineApp` so the collector feeds the one detector. Gated on the existing `route.detect` flag (no new knob); graceful-degrade on no calendar access. Deferred: attendee email-vs-name matching subtleties, live osascript verification, the reserved chunk-transported `source:'calendar'` path. Topic-drift explicitly out of scope. |
| Δ-gate for screen frames (threshold vs OCR cost) | P4B+ (built) | `client/capture/frame-delta.ts` — the pure per-display `FrameDeltaGate` compares a 32px probe with the last KEPT frame, accumulates gradual change, stamps `ScreenFrameMeta.deltaScore`, skips sub-threshold frames before transport, and sends a bounded heartbeat; `shell.ts` owns the production gate and resets it per session. Unit coverage fixes first/static/drift/display/reset/heartbeat semantics. #175's owner-run live gate schedules real capture attempts independently of result completion at a configurable 3–6s cadence, requires an unchanged-frame skip with no new result, and records the cadence/queue evidence; the owner-present rig run passed with 5001–5006 ms actual intervals and two unchanged-frame skips producing no results. |
| LAN sweep discovery (with permission) | future | `engine/fabric/discover.ts` — a consent-gated subnet sweep (cross-host rigs). Blocked on macOS Local-Network TCC (GUI-domain LaunchAgent; see ARCHITECTURE §8 platform note) |
| macOS Keychain secret store | P7 | `engine/fabric/secrets.ts` — `KeychainSecretStore` behind the `SecretStore` interface (drop-in for the v0 file) |
| Drift steering (comparator + chains) | P5 | `engine/voice/{comparator,chains}.ts`; card/glyph = HUD blocks |
| TTS whisper (chain terminus) | P5 | `fabric` tts slot + chain step `tts` |
| Explore canvas (lenses, branches) | P6 | `workbench/explore/` + `engine/surfaces/custom-blocks.ts` |
| Palette designer (buttons, input toggles) | P6 | `client/surfaces/palette/` over action documents |
| Surface WYSIWYG / mode canvas / dial editor | P6 | `client/surfaces/editor/` — forms over documents, no new engine capability allowed |
| Typed queues + envelope/ETA | P4A slice 3 (built) | `engine/queue/kinds.ts` (classify a spooled chunk → audio/screen/llm-work by source/contentType; focus excluded as ephemeral routing context) + `engine/queue/eta.ts` (pure backlog projection from observed drain samples — `basis: 'observed'\|'none'`, no fabricated ETA). `GET /queue` (`QueueStatus`) gains additive `byKind` (per-kind depth) · `eta` (etaMs/caughtUpBy + measured tok/s context) · `overflow` (declared policy from the active mode; only `queue-for-idle` enforced in v0). Measured-tok/s + overflow injected from `api/http.ts` as read-only seams (queue keeps zero fabric/store imports). |
| Keep time — lag metric + capturedAt + delay disclosure | #102 (built) | Delayed data never presents as real-time, made STRUCTURAL·MEASURABLE·VISIBLE. Contracts (additive): `BacklogLag` (`api/payloads.ts`, `$id`) + optional `QueueStatus.lag` (behindMs = now − oldest-pending WORK-chunk capturedAt; oldestPendingCapturedAt?; basis capture-time\|unknown; ABSENT when caught up) — the backward-looking companion to `BacklogEta`; `OcrResult.capturedAt` (`records/ocr.ts`, optional) threaded from `CaptureChunk.capturedAt`. Engine: `queue/spool.ts` `status()` folds the oldest pending work-chunk capturedAt during the existing byKind parse then `computeLag` (focus excluded, single reused `now`); flows through GET `/queue` + `queue.updated` + the `queue` query source unchanged. `screen/processor.ts` `persist()` stamps `capturedAt`. Client: `surfaces/blocks/queue.ts` renders "processing ~Ns behind" only when basis capture-time and behindMs ≥ threshold (default 5s, per-surface override via block query param `lagThresholdMs`); caught-up/sub-threshold/unknown ⇒ silent. Disclosed: capturedAt is always spool-recoverable so no spool-write change (unknown basis is a defensive corner); no per-user inform-config UI (design-session territory); lag is overall, not per-kind. |
| Backlog analytics surface | P7 | `workbench/analytics/` reading `engine/queue/eta` (the eta module now exists — P4A slice 3) |
| Dynamic to-do seam (prompt engine v0) | P4A slice 4 (built) | `engine/act/todo.ts`: `task-extract` act rides the DRAIN (best-effort, gated `act.tasks`) — CONSTRAINS a session's accumulated distillates+moments into a `TodoList` document (deduped, provenance-stamped); a follow-up draft UN-CONSTRAINS it via the `{{todo}}` template variable (empty→omitted). Editable document: `GET/PUT /todos[/:id]` (no flag). The executor gains a `drainActs` map for drain-triggered acts. Prompt engine v0 = one variable resolved from one document; conditionals/iteration/typed-catalogue deferred. |
| Custom user blocks (HTML, sandboxed) | P6 | `engine/surfaces/custom-blocks.ts` + client webview host |
| Mobile / second-screen readouts | unplanned | a new `apps/*` consumer of the same API — the seam already permits it |
| Rust hot paths (if ever needed) | unplanned | replace an engine module behind its contract; `shared/contracts/schemas/` is the port surface |
| Engine-skew refusal + build stamp (basics wave S6) | basics-D (built) | The silent adoption fix: `client/main/engine-supervisor.ts` `assessEngineSkew` (pure) judges an adopted engine's /health version+build against the app's own — a different version, an engine that reports NO version (predates the field), or the SAME version with a different build sha ⇒ skew. Default = REFUSE: `shell.ts ensureEngine` skips the seed (Start/End stays disabled, no session/capture/fabric read from it), the tray LEADS with "⚠ engine refused — version mismatch", and the System window auto-opens with a blocking banner. `OPENINFO_ALLOW_ENGINE_SKEW=1` (opt-in token, `parseAllowSkew`) adopts anyway with a warning (dev pairs). `engineStatusLine` now surfaces the /health `build` (was dropped). **System face** (`client/main/system-face.ts`, pure `model → self-contained data-URL HTML` via the block-renderer vnode serializer): app + engine version/build + the skew banner, opened by the tray "System info…" item (`open-system` command). **Build stamp:** `package.mjs` `resolveBuildSha`/`writeBuildStamp` ship `build-stamp.json` as an extraResource; `shell.ts` reads it (`readBuildStamp`), shows it, and forwards it to the spawned engine as `OPENINFO_BUILD` so /health echoes the same sha (the DMG path — the deploy path already stamped the launchd plist). **Release-protocol fix:** `tools/bump-version.mjs` bumps root+client+ENGINE package.json in lockstep (the engine was the half a manual cut forgot — 0.0.6 app shipped a 0.0.5 engine), `tools/check-version-parity.mjs` + `client/main/version-parity.test.ts` guard against drift. Tests: client +31 (supervisor skew/build/stamp, system-face, tray skew+System item, parity guard). |

## 4. Spikes — what they are and how they bend the plan

A **spike** is a throwaway proof that answers exactly one question, in hours, in `spikes/`, with none of the
production rules applied (hardcode anything, skip types, no tests). It is deleted or graduated-by-rewrite;
its output is an *answer*, recorded as a paragraph in the relevant phase doc.

Standing spike list (each retires a named risk from IMPLEMENTATION §5):

| Spike | Question | Retires |
|---|---|---|
| `seam-echo` | client on machine A → engine on machine B, capture round-trip + offline spool | P1 risk |
| `glass-capture` | does glass's mic/screen/audio transplant build on current macOS/Electron? | P1 risk |
| `extract-quality` | record 10 min of a real meeting (fixtures tool), run 3 local models over the moment/entity schemas — which is usable? | P2 risk |
| `delta-gate` | screenshot Δ-diff: threshold vs OCR cost on real screen activity | P2/P3 |
| `register-detect` | can a small model score detected charm/wit vs the dial scale at all? | P5 risk |
| `sandbox-bridge` | postMessage API-allowlist webview: can a custom block be both useful and contained? | P6 |

### The honest recalibration you asked for

You're right: the first slice is **hours, not days**. The 6-month figure in IMPLEMENTATION.md conflates two
different clocks, and it's worth splitting them permanently:

- **Construction time** — writing the code. With agent-driven development this compresses ~10×: the Phase 1
  seam is a day-or-two build; the Phase 2 HUD vertical is a week-scale build, not six.
- **Convergence time** — the part that does not compress: dogfooding against *real* workdays. Extraction
  quality, router attribution, register detection, drift thresholds — these are tuned against lived meetings,
  and a week of calendar time contains only a week of meetings. Exit criteria like "attend a real meeting,
  trust the ledger" are experiential, not constructional.

Revised expectation per phase: **demo-able in days, trustworthy in weeks.** The phase *order* and *exit
criteria* in IMPLEMENTATION.md stand unchanged — what changes is that construction stops being the bottleneck
after week one, and the critical path becomes fixture data + dogfood cycles. That is why `tools/fixtures`
(record a meeting once, replay it deterministically) is in the tree at P0-priority: it converts convergence
time into regression tests and is the single highest-leverage tool in the repo.

Practical cadence this implies: run spikes `seam-echo`, `glass-capture`, and `extract-quality` in the first
days (they are cheap and they de-risk the two riskiest phases before any production code hardens); build
Phases 0–2 as fast as agents allow; then let the calendar do what only it can — accumulate real sessions —
while construction runs ahead on Phases 3–4 behind flags.

---

### Basics wave A — window shell (S1/S4/S5, branch feat/basics-a-window-shell)

- `apps/client/src/main/window-options.ts` — **S1** per-surface `focusable` override (`SurfaceWindowConfig.focusable` + `hudWindowSpec({focusable})`, ON for `surf-openinfo-chat` so the typed-in HUD window can become key — else macOS NSBeeps every keystroke); `surfaceWindowSpec(surfaceId)` the ONE resolver of chrome+width+focusability shared by the factory + e2e. **S4** `windowTitleFor(surfaceId)`→`openinfo — <Face>` (FACE_NAMES + humanized fallback). **S5** `surfaceWindowWidth`, `MIN_HUD_FIT_WIDTH`=260, `windowContract`/`assertWindowContract` — the window contract (resizes OR provably-fits + self-identifies).
- `apps/client/src/main/shell.ts` — `createSurfaceWindow` now resolves via `surfaceWindowSpec`, `assertWindowContract`s at create (policy item 3, enforced in the ONE factory), and stamps `windowTitleFor` (S4).
- `apps/client/src/surfaces/hud/dev-entry.ts` — **S1** ONE height authority per window (panel surface → PanelController, else auto-resize; no longer both); **S4** renderer drives `document.title` from the loaded surface's live `name`.
- `apps/client/src/surfaces/hud/styles.ts` — **S5** `.hud` fluid (`width:100%;max-width:660px;min-width:0`) so a narrow window never both-edges-clips; wide default HUD still 660.
- `apps/client/hud.html` — **S4** neutral shared `<title>openinfo</title>` (was `— HUD`, mislabeling every framed window).
- `apps/client/scripts/chat-input-e2e.mjs` (`test:e2e:chat`), `apps/client/scripts/clip-e2e.mjs` (`test:e2e:clip`) — driven REAL-Electron e2es (GUI-only, not in headless `test`): real keystrokes land + panel-extent height; narrow windows fit with no clip.

### The pill — Standard App window assembly (Listen/Ask faces, docked panels, branch feat/pill-window)

The MVP centerpiece: ONE glass-parity window — a compact header rectangle (Listen / Ask / Show-Hide / settings-on-hover) with a docked panel beneath. An ASSEMBLY of existing organs; no new block type, no contract change (the `hud` face kind + surface docs express it).

- `apps/engine/src/surfaces/pill.ts` — `defaultPillSurface` (`surf-openinfo-pill`): the Listen glance stack (`now`/`relevant-now`/`moments`/`fields`, mirroring the HUD) + below-panel geometry (collapsed 56 = the BAR, expanded 432 = the ASK panel ~3× the bar); seeded FIRST in `SEEDED_SURFACES` (defaults.ts). `surf-openinfo-hud` stays a standalone/cloneable template.
- `shared/contracts/examples/bundle.standard-app.json` — the Standard App `hud` face surfaceRef → `surf-openinfo-pill` (the pill is the always-on glance face). `bundles/defaults.ts` grew a second `PREVIOUS_DEFAULT_BUNDLE_BODIES` entry (the current hud→hud + `screen` body) so an unedited v1 install refreshes onto the pill (#130 unedited-only; a user edit never clobbered).
- `apps/client/src/surfaces/hud/pill-layout.ts` — `createPillRenderer` (signature-compatible with `renderSurface`, selected by surface id in dev-entry like the note-taker): the header rectangle + docked panel. Listen face = this surface's own glance stack through the SAME `renderSurface`; Ask face = the bundle-RESOLVED chat surface (its `input` block); the Hud's #58 live-transcript strip is composed onto the frame (CSS parks it under the panel, hides it on Ask). Ask is honestly DISABLED until its bundle face resolves; unresolved/absent ⇒ visible text, never a blank panel. Verbs `pill-face`/`pill-toggle`/`pill-settings`.
- `apps/client/src/surfaces/hud/pill.ts` — `PillController`: the window's ONE height authority — THREE extents (bar/listen/ask, the three-state extension of the two-state PanelController) over the SAME `hud:panel-size` bridge. Selecting a face opens the panel; Show-Hide collapses to the bar. Pure `pillHeight`/`pillExtentsFromPanel` + injected bridge, asserted headless.
- `apps/client/src/surfaces/hud/pill-styles.ts` — the pill chrome (scoped `.pill-app`), injected alongside hud/panel styles.
- `apps/client/src/surfaces/hud/dev-entry.ts` — selects the pill renderer + installs `PillController` (instead of PanelController/auto-resize) for `surf-openinfo-pill`; resolves the Ask face FROM THE BUNDLE at open (`chatFaceRefForPill`/`resolvePillAskSurface`: GET /bundles → chat face surfaceRef → its surface doc) so a different bundle yields a different pill; exposes `window.openinfoPill`. `hud.ts` `rerender()` repaints for the client-local face toggle.
- `apps/client/src/surfaces/block-renderer/mount.ts` — `pill-face`/`pill-toggle`/`pill-settings` wired verbs (client-local, the mute-system-stream precedent); the interaction lint blesses them via `WIRED_VERBS` and scans the served pill frame.
- `apps/client/src/main/preload.cts` + `main/shell.ts` — `openinfoShell.openSettings` → `hud:open-settings` opens the EXISTING settings path (the same GET /settings the tray opens; not a new settings UI). `main/window-options.ts` — `surf-openinfo-pill` = glass chrome + focusable (Ask input) + FACE_NAMES self-identity.
- `apps/client/scripts/pill-e2e.mjs` (`test:e2e:pill`) — driven REAL-Electron e2e: self-identifies; Ask resolves from /bundles; faces toggle the real window height (LISTEN 300 / ASK 432 / BAR 56); typed send end-to-end; Show-Hide collapse; settings path fires.

### The pill visibility fix wave (⌘\ anchor · fill · reopen dedupe · anchor-path e2e — branch fix/pill-visibility)

The owner's 0.0.14 live-QA could not find the pill. Three root causes, fixed exactly; the anchor-path e2e is the policy lesson made durable.

- `apps/client/src/main/config.ts` — `DEFAULTS.surfaceId` repointed `surf-openinfo-hud` → **`surf-openinfo-pill`**: the ⌘\ accelerator + tray Show/Hide act on the anchor window `createHudWindow` opens from this default, so the pill (the MVP glance face) is now what the user summons instead of the old hud. Nothing persists `surfaceId` (client.json/window-state/apps-state), so no migration; `OPENINFO_SURFACE` still overrides for dev. `createSurfaceWindow` resolves the pill's full spec (hud chrome + focusable), the `PillController` is its sole height authority, and `showHud`/`hideHud` only toggle visibility — never fighting the panel extents. Pinned in `config.test.ts`.
- `apps/client/src/surfaces/hud/pill-styles.ts` + `hud/dev-entry.ts` — **the microsquare fix.** `.pill-app` gained the `.hud` fluid-fill invariant (`width:100%;max-width:660px;min-width:0`) and `.pill-bar` the `.hud` glass-card idiom (`--s-glass` fill, backdrop blur, `--s-line` border, 15px radius, floating shadow) so the collapsed pill reads as one solid card, not naked floating buttons. But `width:100%` alone left `.pill-app` at 293px (the mount `<div>` is a flex item of the centered `.stage` and shrink-wraps to the pill's narrow content), so dev-entry tags the pill's mount `.pill-mount` and pill-styles gives it `width:100%` — a definite width the pill fills. Measured after: `.pill-app` = 660px at bar/listen/ask (was 293).
- `apps/client/src/main/shell.ts` — `reopenPersistedApps` already deduped the anchor (`if (surfaceId === cfg.surfaceId) continue`, keyed to the exact id `createHudWindow` builds the anchor from), so repointing the default makes a QA-era persisted `surf-openinfo-pill` entry skip automatically instead of opening a DUPLICATE second pill; comment made explicit. No functional change beyond the repoint; the reopen feature (restoring a left-open note-taker) stays intact — `openApps` is not cleared.
- `apps/client/scripts/pill-e2e.mjs` (`test:e2e:pill`) — **SCENE 0, the anchor path.** POLICY (script header): a driven e2e enters through the user's ENTRY POINTS, not past them — scenes 1-6 drove the pill window directly and stayed green through the anchor gap. Scene 0 resolves the config the way boot does (`resolveShellConfig({})` → shipped DEFAULTS) and builds the anchor exactly as `createHudWindow` does, then asserts the pill loads there (not the old hud), `.pill-app` FILLS the window at bar/listen/ask (rect ≥ window − stage margin — the microsquare guard, forever), and the anchor show/hide preserves the pill height. Full driven set green 9/9.

### The Ask-face resolve race fix (retry · true disabled reasons · race-path e2e — branch fix/ask-face-resolve-retry)

The packaged shell creates the pill window BEFORE `ensureEngine()` spawns the bundled engine, and the Ask-face resolve was a one-shot — losing that race left the Ask button (the door to the whole chat box) dead on every packaged cold boot, with a tooltip that lied about why. Fixed with the boot controller's own posture.

- `apps/client/src/surfaces/hud/dev-entry.ts` — the Ask-face resolve now runs through `createAskResolveController`: the boot controller's capped backoff (`backoffMs`) retries `resolvePillAskSurface` until it succeeds (⇒ `setAskAvailable(true)` + rerender, exactly as before), stopping ONLY on the new typed `NoChatFaceError` — thrown when GET /bundles ANSWERED and the data says this pill's bundle has no chat face (a data answer; retrying is pointless). The transient/terminal distinction is a TYPE, never a message match. Every transient failure `console.error`s its attempt (the trace the old one-shot `.catch` never left); the terminal reason paints via `pillSources.chatError`. Asserted headless in `ask-resolve.test.ts` (manual-scheduler idiom from boot.test.ts): fails-N-then-succeeds enables Ask, the ladder caps at 8s forever, no-chat-face settles once with zero retries.
- `apps/client/src/surfaces/hud/pill-layout.ts` — `askDisabledTitle`: the disabled Ask affordance discloses the TRUE current reason — `Ask — catching up, chat will be ready in a moment` while the retry loop is still working; `Ask — this app has no chat face` only after the terminal data answer (the old static title claimed no-chat-face during the mere engine-spawn race). The honest Ask panel's resolving copy is human now ("Catching up — chat will be ready in a moment."). Both disabled reasons covered by the interaction lint + layout tests (the catching-up state must never render the no-chat-face wording).
- `apps/client/scripts/ask-race-e2e.mjs` (`test:e2e:askrace`) — the tenth driven e2e, the ENGINE-LATE race path no prior e2e entered (they all started the fake engine before the window — why this bug shipped green): anchor window at t=0 (Scene-0 entry-point policy), nothing listening; honest boot chip asserted while down; engine listens ~2.5s later; the regression lock — Ask BECOMES enabled once the engine is healthy (verified to FAIL against the pre-fix one-shot) — then the user's Ask click mounts the chat panel + text input at the real 432px extent and typed keystrokes land.

### The pill e2e production window path (#194 — extract the constructor, drive it, branch test/194-pill-e2e-production-path)

The driven pill e2e rebuilt its windows by hand (`resolveShellConfig` + `surfaceWindowSpec` + `new BrowserWindow`) — the config-resolution seam was real, but a regression inside the production constructor's BODY would not have failed the proof (#174 close-out finding). Fixed by refactor-for-invocability, not by documenting the mirror.

- `apps/client/src/main/surface-window.ts` — **NEW**: `constructSurfaceWindow(surfaceId, opts, env)` — the EXACT body the shell's `createSurfaceWindow` factory ran, extracted verbatim (contract assertion → spec resolution → `new BrowserWindow` with title stamp + preload → method-only hardening → renderer observability → position restore → hud.html load with engine/surface/outline params → moved persistence). `SurfaceWindowEnv` carries the caller-bound seams: `engineUrl`/`hudOutline` plus the OPTIONAL shell-state hooks (`pinAuth`, `registerMeta`, `restorePosition`, `onMoved`). Owns the `HUD_HTML`/`PRELOAD_JS` path constants (same dist-relative values shell.ts computed). Main-process-only — the second (and only other) electron-importing module beside shell.ts; headless tests never import it (the pure logic stays in window-options.ts).
- `apps/client/src/main/shell.ts` — `createSurfaceWindow` is now a thin wrapper: `constructSurfaceWindow` + ALL the shell-state hooks bound (`pinTrustedSurface` engine-credential pinning, `windowMeta.set`, `restoreWindowPosition`, `scheduleSaveWindowPosition`). Behavior-preserving extract — no call-site, ordering, or logging change; `HUD_HTML` now imported from surface-window.ts.
- `apps/client/scripts/pill-e2e.mjs` (`test:e2e:pill`) — both windows now come THROUGH the production constructor: Scene 0's anchor is `constructSurfaceWindow(cfg.surfaceId, { chrome:'hud', isDefaultHud:true, startVisible:false }, { engineUrl })` — `createHudWindow`'s exact arguments — and scenes 1-6 build the pill with the Apps-registry shape (`configForSurface(...).chrome`, `startVisible:true`, then `showInactive()`). The header now names the two remaining mirrored seams with their reasons: (a) `SurfaceWindowEnv` hooks left absent — auth is injected at the defaultSession webRequest seam (production's `RendererEngineAuth` needs the shell's configured credential source; the fake engine uses a fixed bearer), and meta/position stores are per-user shell state a throwaway harness must not touch; (b) the `hud:panel-size`/`hud:capture-frame`/`hud:open-settings` IPC handlers stay probe-main mirrors — they live in shell.ts's whenReady wiring, and TCC cannot be granted to a throwaway harness (fake-engine/synthetic-frame posture unchanged, no new capture permissions). Green twice consecutively.
