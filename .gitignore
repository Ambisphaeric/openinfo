node_modules/
dist/
release/
*.log
.DS_Store
*.db
*.db-wal
*.db-shm
spool/

# real personal machine config (IPs, deploy creds) — see .private/README.md
.private/*
!.private/*.example
!.private/README.md

# engine-side secret store (chmod-600 JSON, keyRef values — never for git)
secrets/

# planning material stays on disk only; committed docs describe the system
docs/local/
docs/NEXT.md
docs/RETRO.md
docs/research/
